# E137 — A régua dos 44px fecha: os dois overrides de 24px caem e o `default` mobile é decidido de uma vez

**Rodada 7 (design), sessão 7** · branch `rodada-7-design` · base `3e368a9` (E136)
Fecha: **E8 🟡** (dois alvos de 24×24px por override de `className` — o X do
sino colado no link do aviso, errar por 10px NAVEGA em vez de dispensar) e
**E9 🟡** (o `default` a 36px em qualquer viewport — os 60 alvos que o E92
mediu e adiou). Decide: **P3** pelo default (sim, 44px no mobile).
Suíte: API 873 · frontend 414 · E2E 147 · typecheck verde

## A primeira ação: o número remedido

`/atendimentos` a 390px, ANTES: **153 alvos tocáveis abaixo de 44px** (alturas
20/24/36/38 — os 36 são o `default` do Button; o resto é link de texto e abas,
fora deste escopo).

## O conserto (3 linhas)

1. `button.tsx`: `default` ganhou `min-h-11 md:min-h-9` — 44px abaixo de `md`,
   desktop intacto (o comentário do próprio arquivo já enunciava a régua como
   universal; o E92 adiou, o P3 decidiu).
2. O X do sino: `h-6 w-6` → `md:h-6 md:w-6` — no mobile vale o `h-11 w-11` do
   próprio `size="icon"`. A POSIÇÃO perto do link não muda (cuidado c — o
   conserto é tamanho).
3. O X do checklist de devolução (`reservas/[bloqueioId]`): idem.

## Verificação

- **Depois, remedido**: `/vestidos` a 390px — **0 de 763** alvos abaixo de
  44px. `/atendimentos` — dos **295 Buttons do componente, 2** abaixo: os dois
  são `SelectTrigger` ("Todas as vendedoras/situações"), um PRIMITIVO
  diferente que nunca esteve no achado (nota abaixo); os `default` de 36px
  zeraram.
- **Cuidado a**: reverter é 1 linha se o dono discordar do P3 —
  `min-h-11 md:min-h-9` → `min-h-9`.
- **Suítes**: frontend **414/414** · API **873/873** · **E2E completo
  147/147** · typecheck verde.

## Visto de passagem

- **`SelectTrigger` fica em 36px no mobile** (`ui/select.tsx`, `h-9`) — o
  mesmo raciocínio do Button vale para ele (2 ocorrências em /atendimentos, e
  todos os selects de filtro do app). Fora do escopo dos achados E8/E9 (que
  mediram Button); entrou na tabela de Sobras como **S-D18** — a mudança é a
  mesma palavra (`min-h-11 md:h-9`).
- As ABAS custom (tablist do E97/E130) medem ~37px no mobile — mesma classe,
  registrada na mesma sobra.
