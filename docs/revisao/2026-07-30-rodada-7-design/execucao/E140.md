# E140 — O WhatsApp entra no cadastro inline, no único momento em que é grátis

**Rodada 7 (design), sessão 7** · branch `rodada-7-design` · base `f25cfd5` (E139)
Fecha: **B9 🟡** (o cadastro no clique do combobox mandava só nome + origem — e
a jornada é literalmente "a noiva está NO TELEFONE": o número está na mão da
recepcionista naquele segundo; sem ele, a confirmação das 48h nem é oferecida
e a fila degrada para o lápis "Sem WhatsApp").
Suíte: API 873 · frontend 417 · E2E 147 · typecheck verde

## O conserto (1 campo)

O popover do cadastro inline (`combobox-noiva.tsx`) ganhou um
`Input type="tel"` **"WhatsApp (opcional)"** entre a pergunta da origem e os
botões; preenchido, vai no MESMO POST (`whatsapp` já existia no shape do
lead — conferido no openapi, como o backlog previa); vazio, nada trava. Os
cuidados: a decisão F4 fica de pé (origem continua obrigatória e no mesmo
clique, sem default silencioso), o fluxo rápido continua de 2 campos
obrigatórios, e `type="tel"` sobe o teclado certo (a régua do E92) sem
máscara nova.

## Verificação

- **Antes**: `createLead.mutateAsync({ data: { noivaNome, origem } })` — o
  grep do achado, sem `whatsapp`.
- **Depois, medido de ponta a ponta no app vivo**: cadastro pelo combobox com
  "11 98888-0140" no campo novo → o lead nasce no banco com
  `whatsapp = "11 98888-0140"` (SELECT conferido; o registro do smoke foi
  removido em seguida — higiene S18).
- **Suítes**: frontend **417/417** · API **873/873** · **E2E completo
  147/147** (o spec 51, que exercita o cadastro inline SEM preencher o campo,
  prova o "opcional de verdade") · typecheck verde.
