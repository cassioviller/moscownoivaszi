# E98 — As telas se alcançam

**Rodada 6, sessão 3** · branch `rodada-6/execucao` · base `07a5313` (E97)

**Estado: PARTE 1 de 2.** Fecha **E3 🔴**, F1 🟠, F5 🟡, F9 🟠, F27 🟠, F29 🟠 —
o núcleo que o **cuidado (a) do próprio épico** manda fazer primeiro
("os itens 1 a 4 são o núcleo e pagam sozinhos; o item 10 é capacidade nova e
deve ser a última coisa, cortável sem culpa").

Suíte: **frontend 223 → 230** · API 673 · **E2E 131** · typecheck dos quatro
projetos verde.

---

## E3 🔴 — a tela mais trabalhosa não dizia de quem era o dinheiro

`/financeiro/receber` mostrava quatro linhas visualmente **idênticas**
(`Entrada · vence 16/07 · R$ 1.000,00`), e o nome da noiva só existia no CSV. Quem
recebe precisava abrir o contrato de cada uma para saber a quem pertencia.

O conserto é de leitura, não de dado: **o `contrato.lead` já vinha na resposta**.
O `openapi.yaml` chega a documentar por que ele existe — *"quem é a noiva desta
parcela, sem rebuscar todos os contratos: a cobrança junta por aqui"* — e esta
tela simplesmente não o lia. O nome virou a linha 1 e o link para a ficha; o
`Entrada · vence 16/07` desceu para a linha de apoio.

É o mesmo padrão do C4 no E94 e do F33: o dado estava em mãos e ninguém o
pedia. Vale como sinal de onde procurar da próxima vez.

## F29 — "Atrasadas" mentia sobre a inadimplência

O filtro rodava **sobre a janela do mês corrente**. Quem clicava lia os atrasos
de julho achando que lia a inadimplência inteira: uma parcela vencida em março
simplesmente não existia na tela. É a pergunta de dinheiro mais perigosa do
sistema para se responder pela metade — e a régua certa já existia no produto,
em `/cobranca`, que pede `status: "abertas"` **sem janela**.

Atraso não tem janela por definição: se venceu e não foi pago, é atraso, seja de
ontem ou de dois anos.

Os campos de data continuam na tela e deixam de valer nesse filtro, então a tela
**diz isso** — *"Atraso não tem janela: isto mostra tudo que venceu e não foi
pago, de qualquer mês"*. Escondê-los seria mais limpo e pior: a pessoa precisa
saber por que o número mudou.

## F1 — o link do caminho mais percorrido

"Agendar atendimento" na ficha da noiva. O deep-link `?noiva=` já existia e já
era usado por `reservas/[bloqueioId].tsx`; daqui não havia caminho, então
agendar custava uma navegação de sidebar mais uma busca por nome — **com a noiva
do lado, esperando**. Zero código do lado do formulário.

O gate é `agenda.editar`, não `leads.editar`: quem só edita a ficha não marca
horário.

## F5 — a ficha diz o que falta

`lib/proximo-passo.ts`: regra pura, sete casos de teste. A ficha mostra oito
cards e, quando a noiva é nova, quase todos estão vazios — a vendedora lia os
oito para descobrir que o passo era marcar o primeiro atendimento.

Duas decisões dentro da regra:

**O contrato ativo manda mais que a etapa.** Uma noiva pode estar `EM_PROVAS`
com parcela vencendo: a etapa fala da jornada do VESTIDO, não da do pagamento.
Por isso o contrato é checado antes do `switch`.

**`null` é uma resposta.** Perdida, casamento realizado, devolvido — nada a
sugerir. Uma faixa que aparece sempre vira moldura e ninguém lê, o que
destruiria o valor dela nos casos em que ela importa.

## F9 — o menu era mais restritivo que a própria tela

"Mensagens de hoje" é construída por PARTES: os três blocos (confirmar presença,
cobrar, orçamento vencendo) são gateados separadamente lá dentro, e cada um
funciona sozinho. Mas o item de menu exigia `agenda` — então **quem cuida do
financeiro nunca alcançava a tela que lista as noivas em atraso**, embora o
bloco dela fosse visível para essa pessoa.

`NavItem` ganhou `modulos?: string[]` com semântica de OU (quatro linhas no
`podeVer`), e o item passou a exigir um dos três.

---

## Verificação

- `pnpm run typecheck` — quatro projetos verdes.
- `vitest` frontend: **230** (+7 em `lib/proximo-passo.test.ts`).
- `vitest` API: 673 (o épico não toca o servidor).
- Playwright: **131 passam**, sem nenhuma expectativa alterada — apesar de a
  linha de `/financeiro/receber` ter mudado de forma.

## O que falta (parte 2)

O épico tem 18 achados; esta parte fechou 6. O cuidado (a) avisa que este é "o
épico mais fácil de inchar", e a ordem abaixo segue o dano, não a numeração:

| Item | Achado | Nota |
|---|---|---|
| 1 | E9 🟠 — cabeçalho de detalhe único nas 6 telas de detalhe (breadcrumb, h1, uma hierarquia de ações, status como chip). "Cancelar contrato" sai da fileira de botões, onde hoje o `Badge` rosa "Ativo" é o elemento mais clicável dos três. | é o insumo do `<Breadcrumb>` do E99 — vale fazer junto com ele |
| 3 | F7, F10, F14, F28, F40, F43 — os links restantes: cartão de mensagens no dashboard, "Hoje na loja" clicável, links para Interesses/Lookbook no atendimento em curso, "Receber" na linha da cobrança, "Editar →" nos cards de Configurações, `resumoAcessos()` na linha do membro. | cada um é pequeno e independente |
| 5 | F4 🟠 — `CommandEmpty` do combobox oferece "Cadastrar «{digitado}»". | o padrão do `criarReservaInline` já vive no mesmo arquivo |
| 6 | F12 🟠 — o "Novo Agendamento" da agenda vira link para `/atendimentos/novo?dia=`. Mata ~110 linhas duplicadas, uma classe de prova órfã e a divergência de fuso. | **cuidado (b): confira que o `?dia=` preserva o contexto** |
| 8 | F2 🟠, F3 🟠 — `origem` sem default silencioso (envenena `/noivas/conversao` de forma irreversível) e o badge "Sem WhatsApp" vira link. | **cuidado (c): mexe no `LeadUpdate` da API** |
| 10 | F13 🟠 — a barra de atendimento em curso no `AppLayout`. | capacidade nova; o próprio épico manda cortá-la sem culpa se não couber |

O F12 é o de maior alcance dos que sobraram (apaga uma classe inteira de defeito),
e o item 1 é o único que faz mais sentido junto com outro épico.
