# Lente 3 — a tela e o que ela lê

**2026-08-16** · agente de leitura pura · base até `090cb5d2` (a S-O131 entrou no recorte) · `heliumdb` medido.

## Achados

1. **🟠 Rescindir com "estornar" devolve o dinheiro DUAS vezes — e a tela mostra as duas frases lado a lado sem dizer que são a mesma devolução.** Tela: `pages/contratos/[id].tsx:1445-1456` ("a devolução nasce como conta a pagar da loja, vencendo em 30 dias") e `:1500-1508` (o rádio "Devolvi o valor — estorna R$ X do caixa" continua oferecido). Porta: `contratos.ts:1926-1936` insere a `DEVOLUCAO` sem olhar `destinoPago`; `:1946-1960` zera as parcelas quando `estornar`. Exemplo: R$ 800,00 recebidos, loja cancela, "devolvi": caixa −800 **e** conta a pagar de 800 — R$ 1.600,00 de saída para R$ 800,00 que entraram. **É a cena que o `e2e/63-clausulas-com-gesto.spec.ts:137-147` encena**; 32 linhas de auditoria no `heliumdb`, todas de E2E. Nenhum teste lê `contas_pagar` depois de `estornar`. Conserto: sob `estornar` a conta não nasce — ou o rádio some sob LOJA e a tela diz que a devolução vai pela conta a pagar; e um teste que conte `contas_pagar` depois de `estornar`. **(= lente 1, achado 1; consolidado como UMA sobra.)**
2. **🟡 A mora sugerida e aceita é a de HOJE, mas o diálogo deixa escolher a data em que o dinheiro entrou.** `dialogo-receber-parcela.tsx:109,128-129`; porta `contratos.ts:2531-2534` (`moraDe` sem `hoje`). Exemplo: parcela de R$ 500,00 vencida em 01/07, paga em dinheiro em 01/07, lançada em 15/08 com data 01/07: o diálogo abre com R$ 517,50; se a vendedora digita os R$ 500,00, a parcela fica **PARCIAL devendo R$ 17,50** de uma multa que a noiva nunca deveu. Conserto: mora em `recebidoEm`. **(Irmã do achado 4 da lente 1 — a mesma raiz: a conta é de hoje, o fato é de `recebidoEm`.)**
3. **🟡 A data do recibo sai um dia à frente para recebimento lançado depois das 21h — no papel e na tela do contrato; o portal acerta.** `pages/contratos/[id].tsx:1046` (`diaMesAno` UTC sobre `Recibo.pagoEm`, INSTANTE), `recibo-do-papel.ts:328` (`dataBR` UTC). Pagamento às 21h30 de 28/07 → 28/07 no portal, 29/07 no recibo e na ficha. População: **9 de 303** parcelas com `recebido_em` entre 21h e 23h59 SP. Conserto: `instanteDia` na tela, `dataBRInstante` no recibo.
4. **🟡 (armado, população 0) A tela de Índices gateia por `admin.editar`; a porta guarda `financeiro`** (`configuracoes/index.tsx:282,54` × `financeiro.ts:127`). E a `s36-gate-da-tela-unit` não enxerga porque `gravar` não está em `VERBOS_QUE_ESCREVEM` (`:73-74`). Conserto: gatear por `financeiro.editar` e pôr `gravar` no regex.
5. **🔵 1 porta, 0 telas: `CobrarAtrasoInput.prazoDias`** (`openapi.yaml:6901-6906`; a tela manda `{}`, `[bloqueioId].tsx:546-550`). O formato da S-C98, fechado para a avaria (E232) e não para o atraso.
6. **🔵 A S-C330 pôs "multa, juros e correção" no papel e no carnê, e a tela ainda diz "multa e juros"** sobre um número que inclui a correção: `[id].tsx:1047,1330`, `noiva-portal.tsx:775`, `receber.tsx:310`, `whatsapp.ts:115`, `conciliacao-do-sistema.ts:179`.
7. **🔵 Índices só oferece os últimos 12 meses (`indices.tsx:44`), e a mora pede o mês que faltar, sem teto** — parcela vencida há 14 meses pede um mês sem campo. População 0.
8. **🔵 O dicionário da tela ESCONDE a contagem que a porta devolve** — `admin/index.tsx` `MENSAGENS_ERRO_ADMIN` vence o `detalhe` do servidor ("3 parcela(s), 2 contrato(s), 5 noiva(s)…", `admin.ts:259-263,540-543`).
9. **🔵 Cópia literal da constante (S-C180):** `atendimentos/config.tsx:233-236,703-705` escreve `630`, `1140`, `1080`, `[2..6]` à mão; `EXPEDIENTE_DE_RETIRADA_PADRAO` existe em `agenda-core/expediente-retirada.ts:71-101`.
10. **🔵 Prazo da cobrança do reparo vazio vira 0 dias** — `[bloqueioId].tsx:1765`, `Number("")` é 0; a parcela vence hoje.
11. **🔵 UF da noiva: tela aceita 1 letra, criação recusa (400), edição aceita** — `noiva-form.tsx:91` × `LeadInput.enderecoEstado minLength 2` × `LeadUpdate` sem restrição (S-M9).
12. **🔵 Erro vira silêncio em duas leituras novas:** a fila de atrasos em `contratos/index.tsx:114-127` e o sino (`sino-notificacoes.tsx:126-131`) — 500 = "sem atraso"; a locação da ficha (`noivas/[leadId]/index.tsx:177-181, 458`) some sem frase.

## Conferido e certo

`prazoProprio` ponta a ponta; `iniciativa` viaja; perdoar/restabelecer só onde `podePerdoarMora`; trocar peça (gate = porta, veto da 17ª antes do botão, some depois da retirada); avaria (mesma `avaliarTaxaDeAvaria`, aluguel do payload, gate `vestidos.editar`, congela no recebimento); os sete de `lojas` (E234) 1:1 com validação igual; conciliação E235; manuais; auditoria global; apagar loja/pessoa (próprio usuário sem botão — mais restrito que a porta); desativar regra; fila de atrasos com números do servidor; reajuste e faixa da avaria importam a conta do core; rescisão do GET; E215 (`faltasDaQualificacao` espelha o servidor).
