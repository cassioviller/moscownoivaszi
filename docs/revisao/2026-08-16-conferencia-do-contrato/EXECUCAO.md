# A conferência do contrato — a fila

A tabela de épicos é a **fila**; a de Sobras é a **fonte da verdade do que
continua aberto**. **Conte, não deduza** — a linha aberta é a que não está
riscada.

Plano: [`2026-08-16-conferencia-do-contrato-plano.md`](../../propostas/2026-08-16-conferencia-do-contrato-plano.md)
· Consolidado: [`G-consolidado.md`](G-consolidado.md)
· Lentes: [A](A-dinheiro.md) · [B](B-portas.md) · [C](C-telas.md) · [D](D-e2e-portatil.md) · [E](E-spec-codegen-datas.md) · [F](F-manuais.md) · [G](G-reguas.md)
· Base: `da5148e8`

## O que a conferência abriu

Contado em 16/08/2026: **0 🔴 · 7 🟠 · 17 🟡 · ~30 🔵**, em **8 épicos +
1 lote de higiene**. Duas decisões da dona (E241, E244), nenhuma trava.

**Contado em 16/08/2026 à noite: os 8 épicos E o lote de higiene estão EXECUTADOS** (higiene `484f429b`, 13 de 17 🔵; o resto na S-CF3) (E241 `e9231ce1` · E242 `3029efba` · E243 `c4e152b1` · E244 `d880b43a` · E245 `a736229f` · E246 `f373a65e` · E247 `fb00bd96` · E248 `5096469b`) — **7 🟠 e 17 🟡 fechados, ZERO abertos**; as três decisões executadas na recomendação (E241, E242, E244) foram **confirmadas pela dona em 16/08**, e as sobras da execução (S-CF1, S-CF2, S-CF3) fecharam. **Mas a tabela de Sobras TEM linha aberta**: o `/code-review max` de 16/08 à noite abriu **S-R2 a S-R19 — 1 🔴 · 7 🟠 · 8 🟡 · 2 🔵**, sete delas nascidas da fila deste mesmo dia. **Conte a tabela, não este parágrafo.** **Conte a tabela, não este parágrafo.**

## A fila

| Épico | Tese | Fecha | Estado |
|---|---|---|---|
| ~~**E241**~~ | ~~a rescisão devolve UMA vez~~ | A1=C1 🟠 | ✅ `e9231ce1` · [relatório](execucao/E241.md) — sob `estornar` a conta `DEVOLUCAO` não nasce (o estorno é sempre ≥ a devolução da cláusula, e o clamp "devolucaoTotal − totalEstornado" seria zero sempre); a trilha ganha `devolucaoPorContaAPagar`; a tela troca a frase com o rádio; o `e2e/63` conta `contas_pagar` = 0 — **o `afterAll` da S-O130 já apagava essa conta como sobra de fixture.** Vermelho antes: `expected [ { …(15) } ] to have a length of +0 but got 1`. A régua achou a suíte de API vermelha no `main` desde `ec53e2d6` (S-CF1, `c8dda201`) |
| ~~**E242**~~ | ~~o seed real não inventa índice~~ | A2 🟠, C4, C7 | ✅ `3029efba` · [relatório](execucao/E242.md) — o bloco 7b só corre sob `SEED_IPCA_EXEMPLO=true` (o E2E liga; a instalação real nasce sem índice); **era um PEDIDO da dona (bb03a0f7) e a leitura está na tabela de Decisões**; Índices gateia por `financeiro.editar` nas duas pontas e a s36 ganha `gravar` (reprovou antes: "chama gravarIndiceMonetario sem afirmar gate nenhum"); a lista amplia de 12 em 12. Vermelho antes: `expected [ …(12) ] to have a length of +0 but got 12`. Banco virgem 16 passed |
| ~~**E243**~~ | ~~a mora é do dia do FATO~~ | A3 🟠, A4=C2, C3, C6 | ✅ `c4e152b1` · [relatório](execucao/E243.md) — o `/receber` calcula em `diaLocal(recebidoEm)` (no máximo hoje); porta nova `GET /parcelas/:id/mora?em=` para o diálogo sugerir pela MESMA função (a `mora-na-tela.ts` proíbe recalcular no navegador, e o docblock ganhou); a conciliação deriva a mora dos recibos válidos; recibo e ficha datam em SP (9 de 303); rótulos com "correção". Vermelho antes: `expected [ 33, 5.5 ] to deeply equal [ 30, 5 ]` · `expected 422, got 200` ×2 · `[ parcela, 515 …` |
| ~~**E244**~~ | ~~a 16ª cobra pelo que o papel manda~~ | E1 🟠 (decisão da dona), C5, C10 | ✅ `d880b43a` · [relatório](execucao/E244.md) — `fimPrevistoDaDevolucao` (o papel, senão a janela) nos três sítios; executado na recomendação (o papel), para a dona confirmar; a S-C89 cobrou o primeiro conserto (`expected 9 to be 7`) e a fila passa a data já lida; a cobrança de atraso ganha o prazo na tela; prazo vazio não vira 0. Vermelho antes: `expected 1750 to be 750` |
| ~~**E245**~~ | ~~as portas ao lado~~ | B1, A5=B2, B3–B6, B8 | ✅ `a736229f` · [relatório](execucao/E245.md) — sete consertos, sete cenas de corrida rodadas sobre o código quebrado de propósito (B1 por `pg_locks`); a varredura de trancas cobrou o retrato (45 · 13 · 14); o E225 reprovou a primeira versão do B5 (a devolução se registra no cancelado de propósito) e a guarda encolheu para a saída; B6 declarado como ponto cego, não consertado |
| ~~**E246**~~ | ~~o E2E para de depender do dev~~ | D1–D4, D6–D9 | ✅ `f373a65e` · [relatório](execucao/E246.md) — a fixture da reserva renovada a cada run (respeitando a EXCLUDE), o `13` pelo `href`, o `07` pela busca, contrato próprio no `08` e no `15` (os 4 `skip` saíram), o `16` num ramo só, a competência do `36` em SP, o rastro por tela de `04`/`05`/`51` apagado, as 779 contas do `34`, a data do `52` derivada. E2E 186 em 6,8 min, 0 skipped · banco virgem 16 passed |
| ~~**E247**~~ | ~~as réguas veem a segunda forma~~ | G1–G8, G10 | ✅ `fb00bd96` · [relatório](execucao/E247.md) — cada régua aprende a segunda forma e prova que vê: dois vermelhos literais sobre o repositório como estava (`createParcelaAvulsa (requestBody inline).descricao` sem teto → `maxLength: 1000` no spec; o `52` com `T12:00:00Z` → a âncora da casa); cabines 9 → 12; escritores diretos derivados; a régua do gesto prega a chamada; a cena CANCELADA afirma; as corridas por `sleep` provam a espera; a bomba do `e217` (02/11/2027) desarmada. API 1865 · E2E 186 |
| ~~**E248**~~ | ~~os manuais dizem o que o sistema faz~~ | F1–F8 | ✅ `5096469b` · [relatório](execucao/E248.md) — vendedora (F1, F2, F4), recepção (F4), proprietário (F3, F5, F6, F7, F8); as cinco varreduras dos manuais 29/29 (moldes 11 → 12); PDFs republicados |
| ~~higiene 🔵~~ | ~~lote~~ | B7, C8, C9, C11, C12, E2–E4, G9, G11–G15, A6, A7 | ✅ `484f429b` · [relatório](execucao/higiene.md) — **13 de 17** fechados (A6, A7, B7, C8, C9, C11, C12, E2, E3, E4, G9, G14, dois títulos de G15); **G11, G12, G13 e o resto de G15 ficam declarados na S-CF3** (réguas de propósito grossas, e a régua de prazos é numérica). API 1865 · frontend 1017 · E2E 186 em 7,4 min |

## Sobras

O que aparecer de passagem durante a execução entra aqui **no mesmo commit que
o viu** (regra 12) e sai riscada no commit que a fecha (regra 21).

| ID | Sev. | Onde | O que | Estado |
|---|---|---|---|---|
| ~~S-CF1~~ | 🟡 | `varredura-banco-virgem-cobre-as-migracoes.test.ts:153` | ~~A suíte de API estava vermelha no `main` desde `ec53e2d6` (S-A27): a migração `2026-08-16-s-a27-tipo-de-peca-do-legado.sql` backfilla `vestido_atributos` e não estava na lista travada — `expected [ …(10) ] to deeply equal [ …(9) ]`~~ | ✅ `c8dda201` — a lista sobe de 9 para 10; a tabela já era coberta pelo spec 04. Achada pela régua do E241 (regra 18) |
| ~~S-CF2~~ | 🔵 | `reservas/[bloqueioId].tsx` (`cobrar-atraso`) | Nenhum E2E clica "Cobrar o atraso" — a 16ª pela tela nunca foi encenada, e o campo de prazo do E244 tampouco. Candidata ao lote do E246 | ✅ `206d3e51` — o spec `e2e/65-cobrar-atraso-pela-tela`: a conta na ficha, o prazo (C5/C10), a parcela vencendo no prazo; vermelho medido quebrando a tela (Expected "2026-08-26", Received "2026-08-23"). E2E 187 |
| ~~S-CF3~~ | 🔵 | `varredura-campo-escalar-do-spec`, `varredura-manuais-prazos`, `varredura-das-varreduras`, `sc140`, `so18`, `revisao-reserva-avaria`, `varredura-fixture-do-e2e`, `varredura-expurgo-lgpd` | Os 🔵 da lente G que o lote de higiene deixou DECLARADOS: G11 (o `escrito()` grosso), G12 (os dias vedados da 17ª em prosa sem `data-regua` — a régua de prazos é numérica; ensinar-lhe dias da semana é régua nova), G13 (a heurística da varredura-das-varreduras) e os menores de G15 (alias de SQL pregado, `dataFutura(-n)` como "há n dias", medições em `moscow_base`, o `const r` reutilizado, `coluna:` em comentário). Nenhum tem defeito vivo atrás | ✅ **fechada por DECISÃO da dona em 16/08/2026: ficam grossas de propósito** — a resposta escrita é o fecho (regra 21); reabrir pede defeito vivo, não gosto |

### O que o `/code-review max` achou depois (16/08, noite)

A dona rodou uma revisão `max` do app inteiro sobre `fb3dcb50` — dez ângulos —
**depois** de eu declarar a conferência sem sobra aberta. Ela achou **19**, e a
primeira era que **a suíte de API estava vermelha no `main`** por causa do meu
próprio commit anterior. Conferi cada uma das que seguem contra o código, o
banco ou o SQL antes de escrevê-las aqui (regra 24: o orquestrador confere, não
repassa) — **nenhuma foi descartada**. Sete nasceram da fila de HOJE (E242,
E244, E245, higiene, E248), o que diz o essencial: *a fila que fecha 24 achados
num dia abre outros, e a régua que a mediu não os vê.*

| ID | Sev. | Onde | O que | Estado |
|---|---|---|---|---|
| ~~S-R1~~ | 🔴 | duas varreduras × `e2e/65` | ~~A suíte de API estava VERMELHA no `main` desde `206d3e51`: o spec 65 só entrou nas varreduras que enumeram por `git ls-files` depois do `git add`, e o ponteiro publicou "API 1865 verde"~~ | ✅ `3c71d474` — a âncora vai numa variável (`const casamento`), o retrato sobe para 11; **virou a regra 35 do METODO** |
| ~~S-R2~~ | 🔴 | `reservas.ts` (`PATCH /reservas`) × `financeiro-core/atraso.ts:136` | ~~**Casamento ADIADO não move `contratos.data_devolucao`, e desde o E244 é ela que manda na 16ª.** Mover a data (`PATCH /reservas/:id`) atualiza `bloqueio.casamentoData` e `contratos.dataCasamento` (`reservas.ts:540`) e NÃO a `dataDevolucao` — os únicos escritores são o `POST /contratos` (`:984`) e o spread do `PATCH /contratos`. Exemplo: casamento 12/09 → adiado para 12/12; devolução impressa 15/09; peça devolvida 14/12 (em dia pela janela nova) → `diasDeAtraso('2026-09-15','2026-12-14') = 90 ≥ 10` → **EXTRAVIO, 4 × R$ 3.000,00 = R$ 12.000,00** oferecidos pela fila e cobráveis pela porta, onde antes do E244 dava R$ 0,00~~ | ✅ `458adf11` — o `PATCH /reservas` recalcula as duas datas do papel pela janela nova (E224), preservando a hora; a trilha diz para onde elas foram. **A sobra errava o número para MENOS**: o caput multiplica por PEÇA, e o vermelho medido foi `expected 48000 to be +0`, não 12.000 |
| ~~S-R3~~ | 🟠 | `disponibilidade.ts:276,315` | ~~**O QUARTO sítio da 16ª que o E244 não converteu** (o relatório diz "uma função nos três sítios"). O módulo que pinta o acervo e o calendário segue com `fimUsoPrevisto = casamento + usoDiasDepois` e nunca lê `dataDevolucao`: no dia que o papel dá como prazo, a TELA pinta `ATRASO_DEVOLUCAO` e a PORTA responde 422 `SEM_ATRASO`. O invariante está escrito no docblock que o épico não abriu (`reservas.ts:2321`: *"se as duas divergirem, a tela mostra a peça em dia e a porta cobra o atraso dela"*)~~ | ✅ `458adf11` — `fimUsoPrevisto` virou `fimPrevistoDaDevolucao`; a data entra por dois `leftJoin` no SELECT que `buscarBloqueiosAtivos` já fazia (nenhuma consulta nova) e viaja DENTRO do bloqueio. Vermelho antes: `expected 'ATRASO_DEVOLUCAO' to be 'USO'` |
| S-R4 | 🟠 | `contratos.ts:2955-2969` (perdoar-mora) | **O E245/B3 criou um 200 que mente.** O CAS novo inclui `status`; um recebimento PARCIAL concorrente muda o status, o UPDATE casa 0 linhas, e o fallback só devolve 422 quando `moraDe(...) === null` — com saldo ainda em aberto ele cai no `res.json(200)` com `moraPerdoadaEm: null`. A tela toasta "Multa e juros perdoados", nada foi gravado, não há linha `MORA_PERDOADA` na trilha, e a noiva segue sendo cobrada | aberta (review 16/08) — **conferida no código** |
| ~~S-R5~~ | 🟠 | `indices-monetarios.ts:13` + `heliumdb` | ~~**O E242 gateou o ESCRITOR e deixou o LEITOR cego, sem migração de limpeza.** `ipcaDaLoja` não filtra `atualizadoPor`, e o `heliumdb` ainda tem **11 linhas** `'seed (valor de exemplo…)'` na loja da suíte (+11 na demo) com **110 parcelas vencidas** — a "Correção pelo IPCA (1,58%)" que o épico documenta como defeito continua sendo impressa no carnê, no portal e na trilha da loja que existe. E `scripts/loja-de-demonstracao.ts:636` segue escrevendo exemplos contra `DATABASE_URL`~~ | ✅ `91012acb` — a faxina `2026-08-17-e250-ipca-de-exemplo-sai.sql` apaga pela marca do seed, que virou a constante `MARCA_DO_IPCA_DE_EXEMPLO` (aplicada no `heliumdb`: `DELETE 11`); as 2 linhas que a dona gravou pela tela ficam. **Filtrar o LEITOR, que é o que esta sobra pedia, desfaria a decisão do E242** — a instalação de teste existe para sair com a 9ª funcionando. E o dano VIVO era R$ 0,00: as 110 vencidas são de 2026-07/08 e não têm mês cheio de mora; o defeito está armado, e a prova é R$ 78,96 sobre R$ 5.000,00 vencida em 10/03/2026. O `loja-de-demonstracao` fica, com marca própria e o porquê escrito |
| S-R6 | 🟠 | `contratos.ts:2695` (o A6 da higiene) | **A6 limpou `conciliadoEm` e não o irmão `enviadoContabilidadeEm`** — o par que o E115 declara três linhas do sibling (`:1999-2003`). Parcela enviada com R$ 400,00 e completada com R$ 600,00: o `isNull` do próximo envio (`financeiro.ts:1698`) a exclui, e os R$ 600,00 nunca entram em pacote nenhum. **O conserto óbvio está ERRADO:** limpar o carimbo reenvia a parcela INTEIRA (o envio carimba a linha, não o ato) e duplica os R$ 400,00 já declarados. O conserto certo é o do E235 — envio por ATO — e é épico, não linha | aberta (review 16/08) — **conferida no código; o conserto tem armadilha** |
| S-R7 | 🟠 | `catalogo/[atributoId]/editar.tsx:316-325` (S-O131) | **Apagar opção resolve o alvo por ÍNDICE do `useFieldArray`, que não encolhe.** `fields` é congelado no mount (a `key` é o id do atributo, não há `reset()` nem `remove()`); `opcoesOrdenadas` encolhe no refetch. Depois do primeiro apagar, os dois ficam defasados: o clique na linha rotulada "Marfim" apaga **Champagne**, e o clique na última é um no-op silencioso | aberta (review 16/08) — **conferida no código** |
| S-R8 | 🟠 | `reservas.ts:1266` × `:477/:540` (o B5 do E245) | **O B5 criou o ciclo ABBA que o B6 descreve.** `PATCH /bloqueios` com `retiradaDataReal: null` tranca `contratos` (`:1266`) e depois `vestidos` (`:1309`); o `PATCH /reservas` tranca `vestidos` (`:477`) e depois faz `UPDATE contratos` (`:540`) — ordens inversas, **40P01**, um dos dois cai com 500. O comentário que escrevi diz "na ordem do módulo (contratos → bloqueios → vestidos)" e o código é contratos → vestidos → bloqueios | aberta (review 16/08) — **conferida no código** |
| ~~S-R9~~ | 🟠 | `docs/migracoes/2026-08-16-s-a27-…sql:15-38` | ~~O backfill da S-A27 seleciona o atributo *Tipo de peça* **sem `loja_id`** e faz `FROM alvo, tipo` (cross join): num banco com duas lojas (o dev tem duas "Moscow Noivas" desde a S-O144) escreve 264 linhas em vez de 132, classificando peças de uma loja com o atributo da outra — e a guarda de idempotência então lê tudo como "já classificado". Rodou só no `moscow_base` (uma loja), mas o script fica no repositório para a instalação real~~ | ✅ `91012acb` — `loja_id` nas três pontas (a CTE `tipo`, a guarda de idempotência e o `JOIN … ON` que substitui a vírgula), e os dois SELECT de conferência passam a agrupar por loja. Não é reexecução: numa loja o resultado é idêntico ao gravado. Régua nova `varredura-migracoes-por-loja`, com o vermelho medido sobre o próprio arquivo: `FROM alvo, tipo` |
| S-R10 | 🟡 | `reservas.ts:2884` (o B1 do E245) | A tranca do E245 relê `status` e `atrasoParcelaId`, mas o **VALOR** cobrado vem de `pecasAtrasadasDoContrato(…)` lido no POOL antes dela: uma devolução registrada na janela faz a parcela nascer com R$ 4.250,00 de 8 dias sobre um atraso que já é de 7 — R$ 500,00 a mais. Conserto: chamar a conta depois da tranca, com o `tx` |
| S-R11 | 🟡 | `financeiro.ts:851` (o B4 do E245) | `trilhaDosRecebimentos` não tem parâmetro de executor e lê sempre no POOL — de dentro da transação. Um recebimento novo que commita na janela não aparece na trilha lida, a parcela entra em `derivadas` e ganha o carimbo "conferida" com um ato nunca conferido — reabre o A6 pelo outro lado |
| ~~S-R12~~ | 🟡 | `contratos.ts:1472-1757` (`PATCH /contratos`) | ~~A única porta que EDITA `data_devolucao` é a única de `contratos.ts` que **não chama `derrubarFilaDeAtrasos`** — corrigida a data do papel, a fila e o sino seguem anunciando o atraso (e o valor) por até 5 min. Todas as irmãs derrubam (`:1187, :2156, :2497, :3270, :3335`)~~ | ✅ `458adf11` — derruba incondicional, como as irmãs. Vermelho antes: `expected true to be false` na fila lida logo depois da correção |
| S-R13 | 🟡 | `contratos.ts:972` × `:313/:1123` (o B8 do E245) | A qualificação passou a ser congelada do lead **sob a tranca**, e a guarda `QUALIFICACAO_INCOMPLETA` (`:313`) e o bookkeeping de etapa/perda (`:1123-1133`) seguem lendo o lead do POOL: validação e snapshot deixaram de ser o mesmo objeto. Um CPF apagado na janela nasce contrato ATIVO com qualificação incompleta; um lead marcado PERDIDO na janela commita `CONTRATO_FECHADO` com `perdida_em` preenchido |
| S-R14 | 🟡 | `e2e/64-portas-ganham-tela.spec.ts:104` | Teste que prega o caminho torto (regra 34): espera `apagar-usuario-${estado.adminEmail}` com count 0, e a tela monta o testid com `u.id` — o seletor **nunca existe**, então a única cena de "não apagar a si mesmo" passa mesmo se a guarda for removida |
| S-R15 | 🟡 | `docs/manuais/noiva.html:420` (o E248) | O E248 trocou "multa e juros" por "multa, juros e correção" na vendedora e no proprietário e **deixou o manual da NOIVA** citando a mensagem de WhatsApp na forma velha — que o `whatsapp.ts:115` não escreve mais. As varreduras não pegam: a citação está em `<li>`, fora do `<td>`/`<span class="prazo">` |
| S-R16 | 🟡 | `comissoes/index.tsx:392` | `onAlternarRegra` invalida só a lista de regras; o irmão `onSalvarRegra` (`:372-373`) invalida também a **prévia**. Desativada uma escada, o card da Prévia segue mostrando a comissão calculada com ela — e é esse número que a dona lê antes de clicar em "Fechar competência" |
| S-R17 | 🟡 | `sino-notificacoes.tsx:164` (o C12 da higiene) | O aviso de erro da fila usa id CONSTANTE (`"ATRASO:erro"`) enquanto todos os irmãos derivam o id do conteúdo (`ATRASO:${assinatura}`, `CAIXA:${diaNegativo}`) — e as dispensas moram no `localStorage`. Dispensado uma vez, o aviso que o C12 criou fica mudo para sempre naquele usuário+loja |
| S-R18 | 🔵 | `fila-de-atrasos-cache.ts:101` (o B7 da higiene) | O `derrubarFilaDeAtrasos()` sem argumento sobe a geração só das lojas **já presentes** em `geracaoPorLoja` — para uma loja nunca invalidada pelo nome, a cerca do B7 é inócua. Alcance de teste hoje |
| S-R19 | 🔵 | `admin/index.tsx:294,305` | Apagar loja/pessoa não invalida `useListAuditoriaGlobal` nem o consolidado da rede — os dois cards da mesma tela seguem mostrando o que acabou de sair |

## Decisões da dona

| Épico | Pergunta | Recomendação | Estado |
|---|---|---|---|
| E241 | sob `estornar`, a conta a pagar `DEVOLUCAO` nasce? | não nasce — o dinheiro já voltou pelo estorno | ✅ **confirmada pela dona em 16/08/2026** (`e9231ce1` fica como está) |
| E242 | os 12 meses de IPCA DE EXEMPLO (pedido de 15/08, `bb03a0f7`) valem para a instalação de TESTE (E2E, demo) — a REAL nasce sem índice, e a 9ª fica dita até a dona digitar o IPCA. Confirma? | sim; se quiser os exemplos também na real, `SEED_IPCA_EXEMPLO=true` | ✅ **confirmada pela dona em 16/08/2026: só na de teste** (`3029efba` fica como está) |
| E244 | a 16ª cobra pelo `dataDevolucao` do papel ou pela janela? | o papel — é o que a noiva assinou | ✅ **confirmada pela dona em 16/08/2026: o papel** (`d880b43a` fica como está) |
