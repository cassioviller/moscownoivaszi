# Lente 2 — portas de escrita e corridas

**2026-08-16** · agente de leitura pura · base `090cb5d2` · escopo: os diffs de 41 rotas/libs entre `cd990767` e `HEAD`, contra a disciplina do E171 (`portas-de-escrita.ts:77-182, 403-422`).

Nenhuma porta nova é ABERTA no sentido da varredura; o que segue são frestas na FRONTEIRA — a guarda que uma porta relê e a porta ao lado não, o CAS que repete parte da condição, a tranca implícita que a varredura não conta.

## Achados

1. **🟡 `POST /contratos/:id/cobranca-de-atraso` (E212) copiou o desenho da avaria SEM a tranca que a avaria ganhou dois dias antes.** `routes/reservas.ts:2778-2795` (contrato lido no POOL), `:2831` (`max(numero)` DENTRO da tx e SEM `FOR UPDATE` no contrato), `:2866-2875` (CAS só sobre `atrasoParcelaId`, não sobre `status`). O docblock (`:2755`) diz "mesmo desenho do E97/F22 para a avaria"; a avaria, desde o E159 (`6eb4fda2`), tranca o contrato antes do `max(numero)` pelos motivos de `reservas.ts:2107-2130` (R9/V11) — o E212 nasceu sem eles. Cena A: cobrar atraso × cancelar → **cobrança PREVISTA de R$ 1.450,00 num contrato CANCELADO** (o formato do K7). Cena B: cobrar atraso × receber com mora no mesmo contrato → dois `max = 10`, o segundo `INSERT` morre em 23505 → `REGISTRO_DUPLICADO`, lido como "já cobrei" (V11), e a fila diz `jaCobrada: false`. Conserto: `FOR UPDATE` no contrato antes do `max`, releitura de `status` e `atrasoParcelaId` sob ela — o texto de `reservas.ts:2128-2134`, colado.
2. **🟡 `DELETE /comissao/fechamentos/:id` (S-O121, `199cf3e7`) — "só o último" é lido sob tranca de linhas que ainda não existem.** `comissao.ts:1225-1235`. `SELECT … FOR UPDATE` sobre zero linhas não tranca nada. Cena: A = fechar 08 (tranca as linhas EXISTENTES 06 e 07 e insere 08), B = reabrir 07: posteriores > 07 → nada visível (08 não commitou) → passa; `DELETE 07` bloqueia; A commita; o DELETE prossegue → **07 reaberto com 08 de pé** — o estado que a decisão da dona proíbe, e 08 foi calculado com 07 fechado. Conserto: chamar `trancarFechamentosDasVendedoras(tx, lojaId, [vendedoraId])` logo depois da tranca da conta e SÓ ENTÃO ler `posteriores` (sentença nova, snapshot novo). Ordem preservada. **(É a mesma fresta que a lente 1 achou; consolidada como UMA sobra.)**
3. **🔵 O CAS do `receber` não repete `mora_perdoada_em`, e o do perdão não repete `status`** — `contratos.ts:2599-2604` vs `:2824-2829`. Receber R$ 515,00 × perdoar a mora no mesmo segundo → nasce a linha `MORA` de R$ 15,00 PAGA numa parcela "perdoada", `MORA_PERDOADA` e `MORA_RECEBIDA` na mesma trilha. Conserto: `is not distinct from ${existente.moraPerdoadaEm}` no `where` do receber; `inArray(status, [PREVISTA, PARCIAL])` no CAS do perdão.
4. **🔵 A derivação do carimbo (E235) não exige recebimento** — `financeiro.ts:861-864` (`isNull(conciliadoEm)` só) vs `:797-802` (`isNotNull(recebidoEm)`); trilha lida no POOL dentro da tx (`:851`). Marcar recibo × estornar → P PREVISTA e "conferida com o extrato". Conserto: repetir `isNotNull(recebidoEm)` na derivação.
5. **🔵 As guardas do E231 (retirada desfeita × atraso) e do E223 (troca × retirada) só releem de UM lado** — `reservas.ts:1238-1262` (POOL, sem repetição na tx `:1274-1310`, que tranca só `vestidos`); `contratos.ts:2294-2312` relê sob a tranca do bloqueio, mas o `PATCH /bloqueios` (`reservas.ts:1298-1311`) escreve `retiradaDataReal` sem repetir `cancelado_em IS NULL` → bloqueio cancelado E "na rua", vestido antigo ocupado (predicado do E225). Conserto: `isNull(canceladoEm)` no `where` do PATCH; a releitura do E231 dentro da tx. Caem de graça se o achado 1 trancar o contrato.
6. **🔵 A ordem IMPLÍCITA do `PATCH /reservas` (mover a data) desce a cadeia, e a varredura não a conta** — `reservas.ts:471-474` (`vestidos` FOR UPDATE, 8) → `:501` (UPDATE `bloqueio_vestidos`, 7) → `:537`, `:638` (UPDATE `contratos`, 5). Cancelar vai 5→6→7. Cancelar × mover data → **40P01**, um dos dois cai com 500 (janela de dezenas de ms; sem perda de dado). A varredura da ordem lê só `.for("update")` — trancas implícitas de UPDATE são ponto cego declarável.
7. **🔵 Cache da fila de atrasos (S-C89) armazena resultado velho** — `lib/fila-de-atrasos-cache.ts:79-87`: `derrubar` no meio de um GET, o corpo antigo fica até 5 min. Nenhuma escrita depende dele. Conserto: geração por loja incrementada no `derrubar`.
8. **🔵 O `contrato` do POOL decide a rescisão sob a tranca do status** — `contratos.ts:1751` (pool) → `:1802-1805` (relê SÓ `status`) → `:1907-1922` (`calcularRescisao` com `valorTotal`, `dataRetirada` do pool). Mesmo formato em `:964` (`congelarQualificacao(lead)` do pool `:268`, sob `FOR UPDATE` que só lê `id` em `:880`) — o CPF corrigido pela recepção no meio não entra no instrumento. Conserto: `select()` inteiro nas duas releituras.

## Conferido e certo

Trocar peça (contrato → vínculo relido → bloqueio antigo → vestido novo; ordem 5→7→8); `criarReservaDeVestido` todo pelo `tx`; receber (CAS `status + valorRecebido` e a MORA na mesma tx); estornar; perdoar/desfazer (CAS nos dois sentidos); cancelar (lead → contrato → parcelas; a `DEVOLUCAO` sob a tranca); índices (UPSERT + trilha); conciliação marcar (`onConflictDoNothing` na PK do ato); S-O120; fechar/baixa manual (fechamentos antes de contratos); avaria (avaria → parcela; contrato antes do `max`); reajuste 17ª (`returning()`); diffs de leads/portal/agenda/admin/equipe/orcamentos/vestidos — zero escritas novas.

Nada de 🔴/🟠. Os dois 🟡 são a mesma lição da trilha (a porta ao lado): o E212 nasceu do desenho pré-R9, e a S-O121 apoiou-se numa tranca que só existe quando o que ela quer impedir já está lá.
