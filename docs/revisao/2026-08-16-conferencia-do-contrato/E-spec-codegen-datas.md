# Lente 5 — o contrato entre as camadas (spec, codegen, schema, migração) e as datas

**2026-08-16** · agente de leitura pura · base `090cb5d2` · `heliumdb` conferido; codegen rodado numa CÓPIA em scratchpad para medir drift.

## Achados

1. **🟠 A 16ª conta o atraso do fim da JANELA de uso, e o papel (E224) diz outra data de devolução.** `routes/reservas.ts:2347-2352` (e as grafias irmãs `:2528-2531`, `:2695-2697`): `fimUsoPrevisto = addDias(diaDeNegocio(casamentoData), regra.usoDiasDepois)` → `diasDeAtraso(fimUsoPrevisto, diaDaVolta)` (`atraso.ts:117`). O contrato tem `dataDevolucao` e ela **não entra** em nenhuma das três contas (`git grep dataDevolucao` nas rotas: só PDF, `visao-noiva.ts:312`, `estoque.ts:60`, expediente). `retirada-devolucao.ts:131-133`: a sugestão do E224 anda a devolução para a FRENTE até dia de expediente (36 de 127 reservas caíam em dia fechado; padrão `retiradaDias=[2..6]`, domingo e segunda fechados). Exemplo: casamento sábado 12/09, `usoDiasDepois=2` → janela termina segunda 14/09 (fechado) → o instrumento imprime **devolução terça 15/09 18:00**; na terça de manhã `dias=1` → a noiva entra na fila de atrasos e a 16ª §1º cobra **R$ 500,00 + R$ 250,00 = R$ 750,00 por devolver no dia que o papel manda**. População: 0 de 314 contratos ATIVOS têm `data_devolucao` (o dev é anterior ao E224) — armado, dispara na primeira venda pela tela. Conserto: quando o contrato tiver `dataDevolucao`, o fim previsto é `diaLocal(dataDevolucao)`; senão a janela — uma função só (`fimPrevistoDaDevolucao`) nos três sítios. **Decisão da dona se a régua é o papel ou a janela** (recomendação: o papel — é o que a noiva assinou).
2. **🔵 E217 não tem `docs/migracoes/`.** A migração drizzle existe (`0027_red_the_order.sql`), mas é o único épico com DDL da trilha sem o script (`replit.md`: banco existente "só chega lá por esse script"). Conserto: `docs/migracoes/2026-08-13-e217-*.sql`, `IF NOT EXISTS`.
3. **🔵 A âncora da conta `DEVOLUCAO` diverge das irmãs**: `contratos.ts:1939` grava `inicioDoDia(addDias(diaLocal(agora), 30))` (03:00Z); `reservas.ts:2189/2862/635` gravam `ancoraDeNegocio(...)` (meio-dia SP). Nenhum leitor aplica `diaLocal` a `vencimento` — sem dia errado hoje; segunda grafia da âncora (regra 26).
4. **🔵 Ponteiro morto no spec**: `openapi.yaml:7063` diz que `ACOES_AUDITORIA` vive em `api-server/src/lib/auditoria.ts`; mora em `lib/financeiro-core/src/auditoria.ts` desde a S-O52.

## Conferido e certo

Codegen em dia (orval numa cópia → `lib/api-zod/src` byte-idêntico; 925 `dataDoCorpo()`, 0 `coerce.date()`); enums schema ↔ spec ↔ zod (`parcela_origem`, `conta_pagar_tipo`+`DEVOLUCAO`, `avaria_tipo`, `constatada_em`, `estado_civil`, `IndiceMonetario.competencia`); as 59 `acao: "..."` do servidor com rótulo (as novas incluídas); migrações 0018–0032 ↔ `docs/migracoes/` 1:1 (menos a E217); datas — `prazo_proprio` YMD ponta a ponta, `nascimento` reancorado e lido em UTC, troca/reserva/rescisão/conciliação com a régua certa, `diasDeMora`/`diasDeAtraso` só recebem YMD, `moraDe` com `ipcaDaLoja` nas 8 portas; constantes com unidade certas; o link e o aceite morrem no MESMO instante.
