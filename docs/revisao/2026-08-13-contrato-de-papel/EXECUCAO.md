# O contrato vira regra — a fila

A tabela de épicos é a **fila**; a de Sobras é a **fonte da verdade do que
continua aberto**. **Conte, não deduza** — a linha aberta é a que não está
riscada.

Plano: [`2026-08-13-o-contrato-vira-regra-plano.md`](../../propostas/2026-08-13-o-contrato-vira-regra-plano.md)
· Transcrição: [`A-transcricao.md`](A-transcricao.md)
· Auditoria: [`B-auditoria.md`](B-auditoria.md)
· Base: `f40129f`

## O que foi aberto quando o contrato apareceu

Contado em 13/08/2026:

| natureza | quantos | estado |
|---|---|---|
| Épicos de código (E211–E222) | **12** | **10 executados** (E211, E212, E213, E214, **E215**, E216, **E217**, E218, E221, E222) — **as Ondas A, B e C fechadas** · **1 bloqueado** (E219: a porta que ele guardaria não existe) · **1 travado em decisão** (E220: D4 e D7). Conte os dez nomes, não o texto: esta célula já disse *"9 executados"* listando nove e citando o décimo na frase seguinte |
| Sobras abertas | **45** | **45 já fecharam** nesta trilha. Depois das oito 🟡 de código (14/08), a **rodada de decisões da dona (14/08, todas na recomendação)** fechou 6 por decisão escrita (S-C61, S-C62, S-C131, S-C133, S-C153, S-C171) e a S-C152 fechou com a régua do codegen. Por severidade: **ZERO 🟠**, **4 🟡** (três viraram fila de código pela decisão: S-C60, S-C220, S-C231 — e S-C51 vira o leitor de atos; ver [as recomendações](../../propostas/2026-08-14-recomendacoes-para-o-que-restou.md)), **41 🔵**. **Conte a tabela, não esta linha** |
| Pendências que **não são software** | **4** | abertas (a P4 nasceu no E213) |
| Decisões da dona ainda abertas | **2** (D4, D7) | travam só o E220 |

**Nada trava o começo.** A Fase 0 fechou seis das sete perguntas; as duas que
restam (D4 e D7) são do documento, e o documento é a última onda.

## As quatro ondas, e a razão da ordem

A ordem não é por cláusula nem por valor: é por **de onde vem o dado**.

1. **Onda A — a conta em cima do fato que já existe.** Os três primeiros épicos
   não inventam dado nenhum: o sistema já grava a troca de data, já enxerga o
   atraso na devolução e já sabe qual parcela venceu. Falta a conta. É o
   trabalho mais barato e o único que devolve dinheiro imediatamente.
2. **Onda B — as guardas e os limites.** Regras que apertam gesto que já existe.
   Nenhuma depende das outras.
3. **Onda C — o que o sistema não sabe.** Coluna nova e migração. É a onda cara.
4. **Onda D — o documento.** Depende de tudo acima, porque o PDF tem de imprimir
   os números que o código passou a usar — senão nasce a doença dos manuais: o
   papel dizendo uma coisa e a constante outra.

## Os hashes desta tabela são os do `main`, e três não eram

Os épicos E214, E216 e E221 rodaram em **paralelo, um agente por worktree**, e
cada agente registrou aqui o hash que o `git commit` dele devolveu — `35cce35`,
`3dde7a3`, `6051592`. **Nenhum dos três existe no `main`**: o que entrou foi o
`cherry-pick` do integrador, com outro hash, e em dois casos com o conteúdo
mudado de propósito (a migração `0019` foi descartada e renasceu como `0020` e
`0021`, a terceira colisão da mesma sessão). Os três originais só vivem nos
branches `worktree-agent-*`, que somem quando o worktree é podado — e aí a
tabela apontaria para nada.

**Hash de worktree não é hash.** Quem integra troca os três pelos do `main`
antes de podar, e é o que está feito acima: só `eaa4e90` (E216) tem o mesmo
`patch-id` do original; os outros dois são o patch menos a migração colidida.

---

## A fila

### Onda A — a conta em cima do que já existe

| Épico | Tese | Cláusula | Migração? | Estado |
|---|---|---|---|---|
| ~~**E211**~~ | ~~a data que muda tem preço~~ | 17ª §2º e §3º | rodada e conferida | ✅ `0c8874a` · [relatório](execucao/E211.md) — o reajuste vira PARCELA (não engorda `valorTotal`, senão a base do próximo cresce sozinha) e o degrau vira COLUNA (não contagem da trilha). **O aviso aparece antes do clique**, porque o botão move na hora e a vendedora descobriria a cobrança depois de prometer a data. Duas lições caras: o `drizzle-kit push` **mentiu** ("Changes applied" sem aplicar nada), e a suíte cobrou **5 reprovações que eram todas régua certa** — inclusive a do snapshot, que enumera pelo VERSIONAMENTO e reprovou de novo depois do `generate` |
| ~~**E212**~~ | ~~o atraso na devolução tem preço~~ | 16ª e §§ | rodada e conferida (**origem + coluna**) | ✅ `a88d7ead` · [relatório](execucao/E212.md) — **a decisão da dona vale 6×, e a conta é que a fez**: "um dia de aluguel" é o aluguel **÷ os dias da janela** (R$ 3.000 / 6 = R$ 500), porque com a outra leitura nove dias custam R$ 27.250,00 e o décimo — que é EXTRAVIO, o pior caso do instrumento — custa R$ 12.000,00: **o décimo dia devolveria R$ 15.250,00 ao locatário**. As duas faixas são EXCLUSIVAS (o §1º só vale abaixo do caput) e **o extravio NÃO leva a multa**, que o caput não menciona. O §2º REPARTE: três peças atrasadas pagam três diárias e **uma** multa, numa parcela só — por isso o vínculo mora no CONTRATO. A peça que **nunca saiu** não atrasa; a que **ainda está fora** conta até hoje, e é o que torna esta a única cobrança cujo valor depende do dia do clique — daí a trilha obrigatória. Oito réguas cobraram, e **uma acusou código certo**: a varredura de trancas lia o CAS como porta ABERTA porque `atrasoParcelaId` não estava em `COLUNAS_DE_ESTADO` — a dívida fechou em 12, não 13 |
| ~~**E213**~~ | ~~a parcela vencida tem multa e juros~~ | 9ª | rodada e conferida (**enum + 2 colunas**) | ✅ `fa7d838` · [relatório](execucao/E213.md) — **a Onda A fecha.** A base é a PARCELA e a decisão da dona não é preferência: o **CDC art. 52 §1º** limita a multa a *"dois por cento do valor da prestação"*, e a leitura literal cobraria os 2% do CONTRATO **de novo a cada parcela atrasada** — dez em atraso dariam 20% do contrato numa cláusula que diz 2%. **O plano dizia que faltava a conta; faltava a conta E o teto da porta**: com a 9ª ligada, o `POST /receber` RECUSAVA os R$ 515,00 devidos por uma parcela de R$ 500,00 (`expected 200 "OK", got 422`), dizendo à vendedora que ela cobrava demais enquanto a fila, o carnê e o portal mostravam os R$ 515,00 — **quatro leituras do mesmo número, e a única que decide dizia não**. A imputação quita no principal e **cristaliza só o que entra a mais** (linha `MORA`, PAGA), porque conta DERIVADA não sobrevive ao pagamento do principal: a parcela ficava PARCIAL devendo R$ 15,00 que o sistema dizia não existir. Medido em `heliumdb`, o banco de `DATABASE_URL`: **110 parcelas vencidas, R$ 1.476,00 de multa e R$ 538,25 de juros** que o instrumento manda cobrar. A varredura de trancas **acusou código certo pela segunda vez em dois épicos seguidos** (S-C33), e o relatório abre com o achado sobre si mesmo: **o épico chegou escrito e sem UMA medição** — os vermelhos são reprodução, não gravação |

### Onda B — as guardas e os limites

| Épico | Tese | Cláusula | Migração? | Estado |
|---|---|---|---|---|
| ~~**E222**~~ | ~~o ateliê tem DOIS expedientes e o sistema conhece um~~ | 4ª e 5ª | rodada e conferida (**4 colunas**) | ✅ `31422db` · [relatório](execucao/E222.md) — **a medição mudou o tamanho do épico antes de uma linha de código**: são **723 contratos, 1 com `dataRetirada` e ZERO com `dataDevolucao`**, e **nenhuma tela cita os dois campos** (`git ls-files`) — só se chega neles pela API, que é o formato do E197. Daí as datas continuarem OPCIONAIS e a migração não corrigir linha nenhuma. Não era contradição com o horário que existia — o de lá governa ATENDIMENTO (sete dias até as 20h, do caderno pela S-A8, e **certo para provas**); é ausência, e o modelo tinha um calendário onde o negócio tem dois. O vermelho: `expected 201 to be 422` — o contrato nascia com a peça saindo **num domingo às 23h**. **O PATCH entrou junto** (`expected 200 to be 422`), senão o caminho era fechar no sábado e corrigir para domingo depois. O sábado tem **coluna própria** porque um número só recusaria 18:30 numa quarta ou aceitaria no sábado; o fechamento é **inclusivo** aqui e exclusivo na agenda (prova tem duração, retirar é ato) |
| ~~**E214**~~ | ~~a taxa de limpeza e a de dano ganham faixa~~ | 14ª e 15ª | rodada e conferida | ✅ `0c15cda` + `6d1e860` (a migração renumerada) + `1c439d5` (a régua da moeda, que este épico deixou vermelha no `main`) · [relatório](execucao/E214.md) — as duas cláusulas viram réguas de **formas diferentes** (a da limpeza é absoluta, a do dano é 5× o aluguel DAQUELA peça), e é isso que obriga o `tipo`: sem ele, `custo_reparo` é um número sem régua. O teto sai de `contrato_itens.valor_unitario`, que o sistema tinha e não usava — **R$ 9.000,00 cabem no vestido de R$ 3.000,00 e não cabem no véu de R$ 400,00**. A régua não vira parede — o que VIOLA um número do papel entra com justificativa, gravada na avaria e na trilha. **Peça fora de contrato: a 15ª NÃO alcança o caso** — não barra, e diz que não conferiu (decisão que eu escrevi ao contrário primeiro; medir derrubou o argumento, e a conta está no relatório). **As duas portas conferem**, e a razão entra também na cobrança para não haver beco (apagar a avaria destruiria a foto-prova). Fora do escopo aparente: `listContratos` desce os `itens` no recorte por noiva, e a conta do `ENVELOPE_MAX_BYTES` foi **refeita para dois campos de texto**, não esticada |
| ~~**E218**~~ | ~~a entrada sugere 40% e o plano respeita os 20 dias~~ | 8ª §1º e § único | não | ✅ `f8ab561` · [relatório](execucao/E218.md) — **duas regras do mesmo contrato, tratamentos opostos, e a razão é MEDIDA**: dos 208 contratos ativos com entrada, **101 estão abaixo dos 40%** e a média é **67,6%** — recusar tornaria quase metade do que a loja já fez irreproduzível pela porta, e a entrada é onde a dona negocia. Então a 8ª §1º **avisa** (o placeholder mostra os 40%, a frase diz quanto falta e que dá para seguir) e o § único **recusa**, porque ele garante o dinheiro antes de a peça sair. **A cláusula não vale para toda parcela** — aplicá-la a qualquer vencimento recusaria a avaria (E214), o atraso (E212) e a mora (E213), que nascem depois da retirada; vale para o CARNÊ, e as **duas** portas que o montam conferem (`expected 201 to be 422` nas duas). **O teste achou um defeito meu antes do commit**: a régua misturava o INSTANTE da retirada (que o E222 criou três horas antes) com a data de negócio do vencimento, e as 23:59 de 15/08 em SP viravam 16/08 — a classe da S-O117, agora entre dois épicos da mesma sessão |
| **E219** | **a troca de traje tem prazo** — sem troca após 7 dias, nem às sextas e sábados | 17ª e §1º | não | **BLOQUEADO — a porta que ele guardaria não existe.** O plano diz *"é guarda na porta que edita itens do contrato"*, e essa porta é suposição: enumerado por `git ls-files`, `contratoItensTable` e `contratoBloqueiosTable` recebem escrita em **UM sítio** — o `INSERT` dentro do `POST /contratos` (`contratos.ts:904` e `:916`). Os outros três arquivos que as citam (`portal.ts`, `reservas.ts`, `vestidos.ts`) só LEEM. Não há `PATCH`, `PUT` nem `DELETE` de item: **hoje trocar de traje é cancelar o contrato e fazer outro**. Fechá-lo pede antes um épico de PORTA (trocar peça, libertar a reserva antiga, prender a nova, refazer o snapshot de preço) — decidido em 13/08 que ele espera |

### Onda C — o que o sistema não sabe

| Épico | Tese | Cláusula | Migração? | Estado |
|---|---|---|---|---|
| ~~**E215**~~ | ~~**a ficha guarda quem assina**~~ — os campos que faltavam (estado civil, profissão, RG, nascimento, e-mail e o endereço inteiro). Era o achado de maior alcance da auditoria | identificação | rodada nos DOIS bancos (**13 colunas em `leads`, 12 em `contratos`, 1 enum**) | ✅ `c3adcf93` · [relatório](execucao/E215.md) — **a medição mudou o épico nos DOIS sentidos, o que é novo nesta trilha.** Encareceu: o achado não é que faltava campo, é que **o campo existia** — a tela já oferecia o CPF, ele era `optional()`, e estava em **0 de 735**. É o E222 pelo outro lado (lá o campo existia e nenhuma tela o oferecia), e a conclusão é a mesma: **campo que dá para pular é campo vazio**, então o épico é a REGRA, não a coluna. Barateou: dos que criam contrato, **58 arquivos usam o helper `criarContrato`, que faz `db.insert` direto e não passa pela porta** — a primeira contagem (84+20) não distinguia porta de insert. Sobraram **43 arquivos · 96 chamadas** na porta, e o conserto foi **um lugar**: `criarLead` passou a nascer com a ficha completa, porque desde este épico uma noiva sem CPF **não é "uma noiva simples", é uma que não pode fechar contrato**. Vermelho literal: `expected 201 "Created", got 422` e `expected 'QUALIFICACAO_INCOMPLETA' to be 'DATA_DIVERGE_DA_RESERVA'` — **a guarda nova comendo a asserção de outra guarda**. Três coisas entraram fora do plano: `nascimento` é a **S-O117 num terceiro campo** (meia-noite UTC vira 11/03 em SP, e a única razão de não ter mordido antes é que a coluna não existia); a dívida de `coerce.date()` subiu **839 → 892, o maior salto da história do número**, e **pela primeira vez não é toda inofensiva** — `LeadUpdate.nascimento` aceita `null` para APAGAR, e se o `coerce` o virasse data a noiva teria nascido em **01/01/1970**, congelado no papel que ela assina (medido pela porta: `.nullish()` deixa o null passar antes da coerção); e o snapshot do drizzle reprovou até o `git add`, que é **a lição do E211 repetida por mim**. Duas réguas nasceram, e as duas porque eu as prometi em COMENTÁRIO: `varredura-expurgo-lgpd` (campo pessoal fora do expurgo **sobrevive à anonimização** — lista curada não reprova quando envelhece, ela VAZA) e `qualificacao-espelha-servidor`. E a `enums-do-contrato` **não me conhecia**: `PARES` é curada à mão, um enum de seis valores passaria verde por não ter sido olhado — entrei nela, e ela me **reprovou** por eu ter quebrado o `z.enum` em três linhas |
| ~~**E215** (fase 0)~~ | ~~a decisão da dona~~ | | | **FEITA em 13/08, e ela mudou o desenho.** Medido: **0 de 723 contratos têm CPF**, e não é falta de campo — a tela de fechar contrato **oferece** o CPF (`orcamentos/[id].tsx:1772`) e ele é **opcional** (`z.string().optional()`). São **1397 leads**, e a ficha não tem UM dado civil. Ou seja: o único campo de identificação que já existe está **100% vazio porque dá para pular**, e acrescentar 11 opcionais produziria 11 colunas vazias — a auditoria dizia que a vendedora preenche à mão no papel, e os dados confirmam. **Decisão da dona (13/08): OBRIGATÓRIOS no fecho** — a porta recusa o que falta, nomeando o campo; contratos existentes não são tocados. Ordem obrigatória dentro do épico, senão trava a loja: (1) colunas, (2) a ficha ganha os campos, (3) o contrato congela a cópia, (4) **só então** a obrigatoriedade. E o expurgo LGPD (`routes/leads.ts:390`) é `set({…})` de **lista curada à mão** — a classe da S-C33: campo pessoal novo nasce invisível para ele, e o plano já avisa que *dado pessoal novo entra nas duas pontas ou nasce fora da lei* |
| ~~**E216**~~ | ~~o vestido sabe que é exclusivo~~ | 12ª | rodada e conferida | ✅ `eaa4e90` · [relatório](execucao/E216.md) — a auditoria via **duas** ausências e elas não são do mesmo tipo: *exclusivo* é **marca** (coluna `vestidos.exclusiva`, e a decisão contra o atributo de catálogo é medida — os 9 atributos da loja são todos descritivos, e o catálogo **cascateia no DELETE**), *primeiro aluguel* é **estado** e **já era contável desde o E157** (`GET /vestidos/utilizacao`). Nasce uma coluna, não duas. A leitura da 12ª está declarada: a marca é permanente, o estado expira — a dona corrige em **uma linha**. O aviso nomeia a peça **dentro do diálogo do contrato**, no molde do E211 |
| ~~**E217**~~ | ~~**a rescisão calcula**~~ — reserva nunca volta, 60% de dedução, multa integral na peça exclusiva, devolução em 30 dias, e a **coluna do prazo da 18ª** (campo por contrato, D3) | 8ª §2º, 11ª, 12ª, 13ª, 18ª | sim | ✅ `a2ada8aa` · [relatório](execucao/E217.md) — **a base do "valor integral" é o ITEM** (recomendação do E216, agora decidida): pago se rateia pelo valor dos itens, a fração da peça exclusiva vira multa integral e só a comum segue a 11ª/18ª. Testado com dois itens (R$ 3.000 exclusivo + R$ 500 comum): R$ 857,14 retidos pela 12ª, R$ 57,14 devolvidos pela 11ª. **A 13ª (loja cancela) devolve tudo** — o sistema não mede "serviço prestado" — e ganha o campo `iniciativa: LOCATARIA \| LOJA`, default `LOCATARIA`. Uma `allOf` chegou a existir para modelar a resposta e foi **descartada**: o motor da `varredura-schemas-aninhados` não a resolve, e `comRelacao` caiu em vez de subir — o conserto foi o molde do `Parcela.mora` (E213), campo direto no `Contrato`, escrito por extenso no handler |


### Fora do plano — os que a medição fez nascer

| Épico | Tese | Cláusula | Migração? | Estado |
|---|---|---|---|---|
| ~~**E224**~~ | ~~a retirada e a devolução deixam de existir só na API~~ | 4ª e 5ª | não | ✅ `75fa2cbf` · [relatório](execucao/E224.md) — **fecha S-C35 e S-C36.** O E222 pôs a régua e o campo continuou sem tela: 729 contratos, 1 com retirada, 0 com devolução, 0 telas — e uma quinta linha que não estava em sobra nenhuma, **0 chamadores do `PATCH /contratos/:id` no frontend**. O plano pedia "os defaults da 5ª sugeridos"; a hora era o fácil, **o dia não tinha resposta**, e a óbvia está errada: medido em SQL, **53% das reservas (67 de 127)** teriam ao menos uma ponta em domingo ou segunda, que a 4ª fecha — 22 são casamento de sábado, cuja devolução crua cai na segunda. **A tela entregaria à vendedora um valor que a porta recusa com 422**, então a sugestão anda sempre PARA A FRENTE (para trás tiraria a peça antes do que a reserva segura). **E o épico ACORDA a guarda `CARNE_DEPOIS_DO_PRAZO` do E218, que nunca disparou** por 728 de 729 não terem retirada — o `campo` dela é `parcelas`, que não é campo do formulário, e o recado cairia num toast atrás do diálogo |
| **E223** | **a porta de trocar peça do contrato** — libertar a reserva antiga, prender a nova, refazer o snapshot de preço, e deixar rastro. **Destrava o E219**, que hoje guardaria uma porta que não existe | 17ª | provável | aberto · [plano](../../propostas/2026-08-13-fechar-o-contrato-plano.md) |
### Onda D — o documento

| Épico | Tese | Cláusula | Migração? | Estado |
|---|---|---|---|---|
| **E220** | **o PDF vira o INSTRUMENTO** — as 21 cláusulas, com os números vindos de constantes; e **nasce a validação de CNPJ**, que hoje não existe em lugar nenhum | 6ª, 21ª | não | aberto · trava em **D4**, **D7** e **E215** |
| ~~**E221**~~ | ~~recibo de pagamento~~ | 7ª | rodada e conferida (**índice**, não tabela) | ✅ `fcc24e9` + `dbd3da2` (a migração renumerada) · [relatório](execucao/E221.md) — **o recibo é por RECEBIMENTO, não por parcela**: a cláusula diz "pagamentos EFETUADOS" e uma parcela deste sistema recebe em pedaços, então quem pagou R$ 300,00 em 01/03 tem o papel DE 01/03. **Não nasce tabela**: o ato individual só existe na linha `PARCELA_RECEBIDA` da trilha, escrita na MESMA transação do dinheiro — e o papel **CONCILIA** com a parcela antes de sair (soma maior que o recebido = nenhum recibo, falha fechada). O estorno anula por CORTE, pelos DOIS caminhos (avulso e cancelamento com `destinoPago: estornar`). Achou de passagem que **a trilha não guardava o DIA do pagamento** — só o instante do lançamento. O plano errou duas vezes: o épico **não dependia de D4/D7** (elas são do documento) e a migração não era tabela. Quatro réguas cobraram, e a quarta era defeito DELA: a `e115-migracao-snapshot-unit` lia o COMENTÁRIO do script como DDL — a frase que justifica o `CONCURRENTLY` virou o índice `comum` |

---

## Sobras

**As 🟡 têm plano próprio, medido em 14/08:**
[`2026-08-14-as-oito-amarelas-plano.md`](../../propostas/2026-08-14-as-oito-amarelas-plano.md).
Ele corrige **sete das oito** sobras sobre si mesmas antes da primeira linha de
código — e **quatro agentes em paralelo corrigiram o plano de volta**, o que é o
resultado do dia:

- **O plano errou o vermelho da S-C100.** Ele afirmava que tirar o corte
  reprovaria o `e212-...test.ts:182`; **não reprova** — a fixture de lá é de uma
  peça, a frase dá 147 caracteres, e o corte sobre 147 é inócuo. O assert
  pregava o defeito **na letra e não no efeito**.
- **O plano errou para MENOS na S-C130.** `pages/**` descreve onde os cinco
  estão, não onde o sexto pode nascer: a varredura entregue cobre `src/` inteiro
  (**193 arquivos** contra 73).
- **O plano viu duas telas na S-C120 e são três vozes** — a terceira mandava a
  Recepção *"Fechar o contrato"*, em botão, sobre contrato já fechado.
- **E o lote achou um 🟠 que nenhuma das oito sobras mencionava**: a S-C150, com
  o `GET /financeiro/contas-pagar` respondendo 500 desde o E217.

**Cinco das oito tinham população ZERO**, e a S-C110 segue sendo a única em que
o zero é um relógio: o gesto que a produz nasceu no E224.

| # | O que é | Sev. | Nasceu em | Estado |
|---|---|---|---|---|
| **S-C1** | **O dano constatado na ENTREGA não tem registro.** A 5ª §3º manda a locadora substituir a peça quando o dano é visto **no ato da locação**; o sistema só conhece avaria na **devolução** (`avarias.bloqueioId`). Não virou épico porque é caso raro e a dona não pediu — fica contável | 🔵 | auditoria do contrato | aberta |
| ~~**S-C20**~~ | ~~**Nenhum manual conhece a peça exclusiva.** O E216 pôs o selo em quatro lugares (ficha, acervo, seletor do orçamento, diálogo do contrato) e os cinco manuais de `docs/manuais/` calam a cláusula 12ª inteira. É o achado do **E196** repetido: manual que CALA capacidade é invisível para quem lê — não há como estranhar o que não está escrito, e a `varredura-manuais` passa verde porque confere o MENU, não a prosa. **Não é conserto de passagem**: pela lição do próprio E196, manual se reescreve **depois da onda**, e a onda C ainda tem E215 e E217~~ | 🔵 | E216| ✅ no lote dos manuais (ver S-C96) |
| **S-C21** | **O lookbook público não sabe da marca.** `LookbookPublicoVestido` não traz `exclusiva`, e mostrá-la à noiva no lookbook **é decisão de negócio, não código**: é argumento de venda ("esta peça é só sua") ou é pressão indevida antes de a noiva saber que a rescisão custa o aluguel inteiro? Fica contável até a dona dizer | 🔵 | E216 | aberta |
| **S-C22** | **A peça exclusiva não é filtrável no acervo** — custo declarado da decisão 1 do E216. Os filtros de `vestidos/index.tsx` saem do catálogo (tamanho, coleção, atributos), e `exclusiva` é coluna de propósito. Hoje a conta não dói: são **132 peças em `moscow_base` e ZERO marcadas**. Vale um `Select` a mais no dia em que a loja marcar as primeiras | 🔵 | E216 | aberta |
| ~~**S-C30**~~ | **A régua do PDF carrega um caractere INVISÍVEL.** `e165-pdf-fala-a-verdade.test.ts:19` normaliza o espaço duro do `brl` com o NBSP **literal** dentro do `replace`. Um editor que normalize espaços desliga a normalização em silêncio e o golden test passa a comparar bytes de codificação em vez de texto. Custou dois testes ao E221, que já escreve ` ` escapado. Uma linha  | 🔵 | E221 | ✅ `1aeec41` · [relatório](execucao/S-C30-S-C34.md) — **não virou uma linha, virou RÉGUA.** Eram **9 sítios em 6 arquivos**, e a composição é o achado: 2 em regex de NORMALIZAÇÃO (a classe silenciosa, que desliga a régua sem falhar) e 7 em literais de frase. **3 dos 7 moravam sob um comentário que AFIRMAVA o escape** — o caractere é invisível, então a prosa sobre ele não é conferível por leitura, e um dos três foi escrito no E218 no mesmo dia. Nasce a `varredura-espaco-duro-literal`, que enumera por `git ls-files` com piso de 800 arquivos: grep acha os de hoje, régua impede o de amanhã |
| ~~**S-C31**~~ | ~~O recebimento PARCIAL é datado pelo último pedaço no caixa~~ | 🔵 | E221 | ✅ `59aed54c` · [relatório](execucao/S-C31.md) — **fluxo, CSV do fluxo e DRE datam cada recebimento pelo dia DELE**, lendo os mesmos atos que o recibo do E221 lê. **A população é ZERO nos dois bancos** (`moscow_base` não tem UMA parcela; das 301 vivas com trilha em dev, todas têm **um ato só**, e `dia_difere = 0`): o que estava aberto era o mecanismo, e por isso **não há migração**. Três decisões declaradas: o **total nunca muda** (a parcela só se divide quando a soma dos atos fecha com o `valorRecebido` — senão entra como entrava, porque sumir com dinheiro é pior que datá-lo errado), **um ato só não se divide** (o `recebidoEm` da parcela É o dia dele, e é melhor que a trilha para os atos anteriores ao E221) e **data que não existe não se inventa**. O conserto precisou do que não estava na sobra: **a janela do SQL** — a parcela paga em 28/02 e 15/03 tem `recebido_em` em março e **não entrava** na consulta de fevereiro, então o mês continuaria R$ 300,00 a menos por mais que o motor dividisse. **O fechamento de comissão não corre risco, e isso foi medido**: ele data por `contratos.fechadoEm` (`comissao.ts:442`). **A conciliação ficou FORA por decisão** — `parcelas.conciliado_em` é uma coluna por linha, marcar um pedaço marcaria o outro e **esconderia divergência**, e carimbo por ato pede a tabela que o E221 recusou (S-C51/S-C52) |
| ~~**S-C10**~~ | ~~**Os "61 das 63 avarias" envelheceram, e ainda sustentam decisões de desenho.** O número nasceu no E110 e é citado em **8 sítios versionados**. Medido em `moscow_base` no E214: **116 bloqueios, TODOS com `lead_id` próprio, nenhum sem dono, e ZERO avarias.** Os comentários argumentam com ele, então não é troca de texto — é remedir e decidir se o argumento sobrevive.~~ **FECHADA — e eram 19 ocorrências em 17 arquivos, não 8 sítios** (enumerado por `git ls-files`; três dos que faltavam são justamente os que ARGUMENTAM: `escopo-loja.ts`, `agenda.ts`, `orcamentos.ts`). **O número nunca foi asserção** — as 19 são prosa, nenhuma dentro de um `expect`: não havia teste fingindo 63, havia onze argumentos de pé sobre estatística morta, que é pior, porque prosa não reprova quando envelhece. Remedido nos DOIS bancos: `moscow_base` **116 bloqueios, 116 com dona, 0 sem, 0 avarias**; `heliumdb` **127 `RESERVA_CASAMENTO` (125 com dona, 2 sem) + 100 `MANUTENCAO`, 0 reservas, 0 avarias**. O caso era 97% e é **0 de 116 na loja, 2 de 127 no dev** — e os 2 nasceram em 12/08 às 05:47 e 05:50, resíduo de fixture. **O argumento tinha duas metades e só uma caiu:** a estatística ("parede diária") morreu; a lógica ("sem dona não há o que comparar") é hoje sozinha quem segura o desenho, porque `lead_id` é NULLABLE e a porta de criação aceita o nulo. **Nenhum `if` foi desenhado sobre a metade que caiu** — o que caiu foi a razão de NÃO apertar a porta, e virou a S-C60. As 63 eram vazamento do spec 48, varrido em `3b71a43` (06/08): **o número estava morto havia sete dias quando o E167 o citou**, e a nota dele (`E167.md:60`) diz *"não é remedível nesta árvore, e não precisa ser"* depois de medir o zero e descartá-lo | 🟡 | E214 | ✅ `d6cb2fc9` · [relatório](execucao/S-C10.md) |
| ~~**S-C60**~~ | ~~**A porta que cria bloqueio órfão, e o argumento que a protegia caiu.** `routes/reservas.ts:929` — o `POST /bloqueios` prova vestido, lead e reserva contra a loja, os dois um contra o outro (R5/V4) e a mãe contra o cancelamento (R7), e **nunca exige que uma das duas âncoras de dona exista**: `RESERVA_CASAMENTO` sem `leadId` **e** sem `reservaId` é **201 hoje**, com `donoDoBloqueio` nulo para sempre — 2 dos 127 do dev nasceram assim. Já foi olhado e arquivado uma vez (metade da S27 da rodada 6, conferência de 05/08), e o que o mantinha fechado era o número: recusar seria *"trocar um defeito raro por uma parede diária"*, medida em 97%. **Remedido, a parede tem largura ZERO em `moscow_base`** (0 de 116). Exigir dona passou de caro a barato, e virou decisão de produto — a loja segura a peça antes de saber de quem será? Se não, a coluna vira `NOT NULL` por migração~~ | 🟡 | S-C10 | ✅ E228 `3e2ecd8d` (decisão 14/08: pode segurar, por 7 dias) — o órfão expira: `VALIDADE_DO_BLOQUEIO_ORFAO_DIAS = 7`, o predicado o solta (menos o que já SAIU — física ganha de prazo), a solta vira FATO (`canceladoEm` gravado na transação do criador, porque o EXCLUDE só enxerga essa coluna — foi o achado do épico), e a lista diz o prazo (`orfaoSeguraAte` no spec + selo âmbar). Abre S-C234 🔵 |
| ~~**S-C61**~~ | ~~**Não há régua contra estatística de banco que envelhece em comentário.** Onze argumentos repetiram por **sete dias e três épicos** um número que um commit do próprio repositório (`3b71a43`) havia zerado, com a suíte verde o tempo todo — porque o número é **prosa**. É a classe que o E184 pegou nos manuais e fechou com a `varredura-manuais-prazos` (9 células, 5 constantes), mas aqui não há constante contra o que pregar: contagem de população não vira `const`. O caminho barato não é conferir o número — é **proibir o formato**: comentário que cite contagem de banco sem **data** e sem apontar o sítio canônico reprova. A S-C10 deixou a árvore no formato certo (todo sítio datado, o canônico em `lib/dono-do-bloqueio.ts`); nada obriga o próximo a manter~~ | 🔵 | S-C10 | ✅ por DECISÃO (14/08, na recomendação): **não fazer régua** — régua textual sobre prosa custaria mais que o defeito. A convenção fica escrita: **número de banco em comentário sempre com a data da medição** e apontando o sítio canônico. Quem citar sem data está errado por convenção, não por régua |
| ~~**S-C62**~~ | ~~**O "véu" tem população ZERO nos dois bancos, e é mecanismo de cinco portas.** A derivação de dono pela reserva-mãe (V3/E163 · V14/E167 · S-O17/E179 · S-O56/E185) é um módulo de 6 funções, 5 chamadas em `reservas.ts`, 10 operações que serializam `BloqueioVestido` aninhado, um campo do contrato de API e um spec de E2E. **A população que ela serve é zero nos dois bancos**: em `moscow_base`, 115 dos 116 bloqueios estão pendurados numa reserva-mãe **e todos têm `lead_id` próprio** (a derivação devolve o mesmo com ou sem ela); em `heliumdb` a tabela `reservas` está **vazia** e nenhum bloqueio tem `reserva_id`. Não é pedido de remoção — o mecanismo está certo e o `e2e/62` o exercita. É para ficar contável que o único lugar do mundo onde ele roda é uma fixture~~ | 🔵 | S-C10 | ✅ por DECISÃO (14/08, na recomendação): **o mecanismo fica** — cinco portas o usam e o E185 provou que ele morde; população zero é fato de DADO, não de código. A sobra existia para deixar isso contável, e agora está contado e decidido |
| ~~**S-C63**~~ | **`moscow_base` não recebeu a migração do E214, e é o banco do preview.** Medido em `information_schema.columns`: `avarias.tipo` e `avarias.justificativa_da_taxa` **não existem** na loja, enquanto `contratos.atraso_parcela_id` (E212), `parcelas.mora_perdoada_em` (E213) e `vestidos.exclusiva` (E216) existem. **Quatro das cinco colunas do dia chegaram e a do E214 ficou** — `docs/migracoes/2026-08-13-e214-taxa-de-limpeza-e-de-dano.sql` foi escrito e não foi rodado ali. O preview roda no banco da loja por `APP_DATABASE_NAME` e o `POST /avarias` do `main` escreve `tipo`: registrar uma avaria em `moscow_base` bate numa coluna que não existe. Sem perda de dado (a loja tem ZERO avarias), por isso 🟠 e não 🔴 — mas é a lição do E172 na mesma semana em que foi escrita: **a migração foi RODADA, não só escrita.** O `.sql` é idempotente (`IF NOT EXISTS`); quem fechar confere as duas linhas depois | 🟠 | S-C10 | ✅ **sem hash, e é o certo: fechou rodando o `.sql`, não commitando.** Conferido hoje em `information_schema.columns` do `moscow_base` — `avarias.tipo` e `avarias.justificativa_da_taxa` **existem**. É a única linha desta tabela cujo fecho é um ATO no banco, e por isso a prova é a consulta, não o `git` |
| ~~**S-C32**~~ | **O atraso não tem FILA — só se descobre abrindo a ficha daquela reserva.** O E212 pôs a conta e o botão em `reservas/[bloqueioId].tsx`, e `ATRASO_DEVOLUCAO` **não aparece em tela nenhuma além dela** (medido: um único sítio em `moscow-noivas/src`, e é o comentário que o próprio E212 escreveu). A peça que não voltou soma uma diária por dia em silêncio: quem não abrir aquela ficha não sabe que ela existe, e o valor cresce sozinho. É o oposto do reajuste do E211, que nasce do gesto de mover a data — aqui o fato é a AUSÊNCIA de gesto, e ausência não notifica ninguém | 🟡 | E212 | ✅ `cec523c2` · [relatório](execucao/S-C32.md) |
| ~~**S-C33**~~ | ~~**`COLUNAS_DE_ESTADO` é lista curada à mão, e coluna de estado nova nasce invisível para a detecção de CAS.**~~ A varredura de trancas leu a porta do E212 como ABERTA enquanto o `where` da escrita repetia exatamente a condição lida — só porque `contratos.atrasoParcelaId` não estava na lista. O E212 consertou POR COLUNA (acrescentou a dela), e **nada obriga a lista a ficar completa**: é a classe que a conferência de 2026-08-05 achou na S30 (*"trava a lista, não a contagem"*), agora do lado das colunas. A régua acusa código certo, que é a direção mais cara — quem for fechar troca a lista por um derivado do schema. **O E213 é a segunda evidência, em dois épicos seguidos**: `parcelas.moraPerdoadaEm` nasceu invisível igual, e as DUAS portas do perdão apareceram como abertas estando sob CAS de verdade (`"routes/contratos.ts": 1` virou `3` no vermelho reproduzido). Duas colunas, dois épicos, o mesmo ponto cego — a lista curada não é sustentável | 🔵 | E212 | ✅ `241ee30f` · [relatório](execucao/S-C33.md) — a lista virou derivado do schema (`getTableColumns`), com o **critério declarado**: `status` enum, mais `timestamp` **anulável** cujo nome diz que o ato aconteceu (sufixos `Em` e `DataReal`). **Uma exceção nomeada**, `contratos.atrasoParcelaId`, porque ele é estado escrito como VÍNCULO — abrir o critério para "FK anulável" pegaria junto **cinco colunas de parentesco**, cinco falsos por um verdadeiro na direção que autoriza. **A contagem não se mexeu, e é o resultado**: 303 arquivos · 55 portas · 31 TRANCA · 11 CAS · 13 ABERTA antes e depois, mapa por arquivo idêntico. O derivado enxerga **seis colunas que a lista não tinha** (as três `bloqueio_vestidos.*DataReal`, `lavagemConcluidaEm`, `comissaoEstornadaEm`, `publicoExpiraEm`) e **nenhuma move porta alguma hoje**. Cada metade foi medida TIRANDO-A: sem a grafia do fato datado a dívida vai a **18** (`expected 18 to be 13`) e o CAS cai a 6; sem a exceção, `reservas.ts` vai a 5 e a dívida a **14**. E a terceira vez foi encenada — `parcelas.protestada_em` sintética entrou na conta **sem tocar em teste nenhum**. 29 → **34 casos** |
| ~~**S-C35**~~ | **A retirada e a devolução não têm onde ser preenchidas.** As duas colunas existem desde sempre, a API grava, o PDF do contrato imprime os dois rótulos — e **nenhuma tela oferece o campo** (0 sítios em `artifacts/moscow-noivas/src`, enumerado por `git ls-files`). Medido: **1 de 723 contratos** tem data de retirada, nenhum tem devolução. O E222 pôs a régua na porta; enquanto o gesto faltar, o expediente da 4ª só é exercido por quem chama a API — o formato do E197 | 🟡 | E222 | ✅ `75fa2cbf` (E224) · [relatório](execucao/E224.md) |
| ~~**S-C36**~~ | **O PDF do contrato imprime "Retirada" e "Devolucao" sempre vazios.** `contrato-pdf.ts:128-129` desenha as duas linhas a partir de campos que ninguém preenche. Não é defeito de código: é a mesma ausência da S-C35 vista no papel que a noiva leva para casa, e fecha junto com ela | 🔵 | E222 | ✅ `75fa2cbf` (E224) · [relatório](execucao/E224.md) |
| ~~**S-C34**~~ | **A mensagem de cobrança cobra a multa e não a NOMEIA.** `whatsapp.ts:78` imprime `brl(p.totalVencido)`, e o `totalVencido` passou a incluir multa e juros no E213 (`cobranca.ts:154`). A noiva recebe *"um valor em aberto: **R$ 515,00**, há 30 dias"* de uma parcela de **R$ 500,00** — confere o carnê, vê R$ 500,00, e liga para a loja. É o **oposto** do que o próprio E213 fez no portal, onde o acréscimo vem com a conta escrita ao lado, e pela razão declarada lá: número maior sem explicação é o que gera a ligação. Uma linha na mensagem  | 🟡 | E213 | ✅ `1aeec41` · [relatório](execucao/S-C30-S-C34.md) — a mensagem ganha uma **segunda linha, só quando há acréscimo**: *"Esse total já inclui R$ 15,00 de multa e juros pelo atraso (cláusula 9ª do contrato)."* Ela **não repete o principal de propósito** — subtrair 515 − 15 seria a segunda grafia de uma conta que a fila já faz em centavos (lição do E187). Para nomear, a fila passou a carregar as METADES (`NoivaInadimplente.acrescimoVencido`), e sem elas a mensagem sai como sempre saiu. Tom concierge e a saída para "já paguei" intactos, com assert sobre os dois |
| ~~**S-C50**~~ | **A multa da cláusula 9ª é paga e não gera recibo.** A parcela `MORA` do E213 nasce PAGA na mesma transação do recebimento (`contratos.ts:1833`), mas a linha de trilha dela é **`MORA_RECEBIDA`** (`contratos.ts:1850`) e o recibo só conhece `PARCELA_RECEBIDA` (`recibo-do-papel.ts:67`). A soma dos atos dá **R$ 0,00 contra os R$ 15,00 recebidos**, `confere` é `false` e **nenhum papel sai** — a falha FECHADA do E221 funcionando como projetada sobre um caso que ela não previu. A cláusula 7ª manda fornecer *"todos os recibos de pagamentos efetuados"*, e este é um pagamento efetuado. Achado lendo o E213 contra o E221: nenhum dos dois olhou o outro | 🟡 | S-C31 | ✅ `c9345fec` · [relatório](execucao/S-C50.md) |
| **S-C51** | **A conciliação continua um movimento por PARCELA.** `conciliacao.tsx:147-158` monta um `MovimentoSistema` por parcela, datado pelo `recebidoEm`. Contra um extrato que traz as DUAS linhas do banco (R$ 300,00 em 01/03 e R$ 700,00 em 15/03) a tela produz **três divergências falsas** — duas "só no banco" e uma "só no sistema" — de um pagamento perfeitamente correto. Dividi-la exige carimbo por ATO, e `parcelas.conciliado_em` (`financeiro.ts:617`) é coluna por linha: **marcar um pedaço marcaria o outro**, escondendo divergência, que é o oposto do que a conciliação existe para fazer. Carimbo por ato pede a tabela `recebimentos` que o E221 **recusou por escrito** — é decisão de modelagem, não linha de código | 🟡 | S-C31 | aberta |
| **S-C52** | **O carimbo da contadora ficou meio passo atrás do CSV.** `contabilidade/enviar` (`financeiro.ts:1491`) seleciona por `parcelas.recebido_em`, então a parcela cujos R$ 300,00 saem no CSV de **fevereiro** só é carimbada quando **março** fechar. O carimbo é operacional (é o `isNull` dele que decide o próximo envio) e não histórico — a história mora na trilha —, mas o descompasso **nasceu com a S-C31** e tem de ser contado. Fecha junto com a S-C51: é o mesmo carimbo sem casa por ato | 🔵 | S-C31 | aberta |
| **S-C53** | **Três leituras menores ainda datam pelo último pedaço.** `GET /financeiro/alerta-caixa` (`financeiro.ts:1184`, o saldo desde a âncora), `GET /financeiro/parcelas/exportar` (`financeiro.ts:1437`, a coluna "Recebido Em" do CSV) e o consolidado da rede (`admin.ts:688`, `recebido_em >= inicioMes`). As três ficaram fora do escopo de propósito — as duas primeiras respondem *"a parcela"*, não *"o movimento"*, e a terceira é de outro módulo —, mas as três somam dinheiro por data | 🔵 | S-C31 | aberta |
| ~~**S-C55**~~ | **`NOMES_NO_BANCO` é a MESMA classe da S-C33, e não foi tocada.** `portas-de-escrita.ts:77` mapeia à mão o nome de cada tabela quente no Postgres, e é ele que a peneira de SQL cru usa (`:963`, `new RegExp("\\b" + NOMES_NO_BANCO[q] + "\\b")`). **Um nome errado ali faz a regex nunca casar e a peneira devolver `[]`** — verde por não ter olhado, que é o pior resultado possível numa sonda e é literalmente o que a abertura daquele arquivo diz não querer ser. Os cinco batem hoje, conferidos contra `getTableConfig(t).name`; o que está aberto é o mecanismo. O `getTableConfig` já está importável no mesmo arquivo desde o fecho da S-C33 — a conversão custa uma linha, e o teste que a prega é o mesmo formato do de exceções | 🔵 | S-C33 | ✅ `edfaeba7` · [relatório](execucao/S-C46-S-C55.md) |
| **S-C56** | **O critério da S-C33 aceita o PRAZO junto do FATO.** `orcamentos.publicoExpiraEm` é `timestamp` anulável terminado em `Em` e entra pela grafia, mas responde *"até quando"*, não *"já aconteceu"*. Hoje não muda nada (nenhuma porta a repete no `where` nem a projeta na tranca), e a inclusão é na direção que **AFROUXA**: o dia em que uma escrita puser `publico_expira_em` no `where` sem ter lido o valor, a varredura a promove a CAS de graça. Uma exceção de REMOÇÃO resolve, e ela **não entrou no fecho de propósito** — exceção sem defeito medido é a lista curada nascendo de novo, pela outra ponta. Quem fechar decide entre a exceção nomeada e uma terceira peneira; a segunda é pior, porque enumera verbos | 🔵 | S-C33 | aberta |
| ~~**S-C11**~~ | ~~**A avaria não tem porta de EDIÇÃO.**~~ `custo_reparo`, `tipo` e `justificativa_da_taxa` só entravam no `POST` de nascimento; quem digitou R$ 1.500,00 onde eram R$ 150,00 só tinha o caminho de apagar e refazer — que o E115 recusa sob cobrança viva e que leva a foto-prova junto | 🟡 | E214 | ✅ `9fa70a59` · [relatório](execucao/S-C11.md) — **o `PATCH /avarias/:id` nasce, e as três decisões são de dinheiro**: a régua do E214 é reconferida na edição (senão bastava nascer com R$ 400,00 e corrigir para R$ 9.000,00); havendo cobrança viva o teto sai do contrato que **cobra** e `parcelas.valor_previsto` **acompanha** na mesma transação (a ficha e o carnê não podem dizer números diferentes); e **dinheiro que entrou congela a linha** (409 `AVARIA_COM_RECEBIMENTO`), com o caminho de volta dito — estornar e então corrigir. A assimetria com o `DELETE` é o julgamento: apagar recusa em QUALQUER cobrança viva porque a FOTO sustenta a parcela; corrigir recusa só onde houve recebimento, porque mexer no previsto de parcela que ninguém pagou não move um centavo. Vermelho literal: **16 reprovações, todas 404**, uma delas `expected 'ROTA_NAO_ENCONTRADA' to be 'AVARIA_NAO_ENCONTRADA'`; verde em **17**. População medida antes de desenhar: **zero avarias nos dois bancos**, o mesmo formato do E185. A porta nova é **TRANCA** medida (`trancou=[avariasTable,parcelasTable]`, degraus 3→6, `releitura=tranca lê parcelasTable.status`, `cas=[recebidoEm]`), e **tudo é decidido SOB a tranca** — ler no pool e conferir depois tem a janela que o V15 mediu no `DELETE` ao lado. E o achado sobre a régua: a `varredura-portas-sob-tranca` **passou verde** sobre a porta nova, porque os pisos dela são `>=` — remedida, ela dá **56 · 32 TRANCA · 11 CAS · 13 ABERTA** contra os *48 · 31 · 8 · 9* que a prosa dela e o `replit.md` diziam, e **sete das oito diferenças são de E212/E213/E216/E221** |
| ~~**S-C45**~~ | ~~**Os cinco manuais não citam avaria NENHUMA** — 0 ocorrências de "avaria" em `vendedora.html`, `costureira.html`, `noiva.html`, `recepcao.html` e `proprietario.html`, enumerados por `git ls-files`. O caminho inteiro da devolução com dano — registrar com foto (E71), a faixa das cláusulas 14ª/15ª (E214), cobrar no carnê (E97) e agora corrigir (S-C11) — está fora da documentação de todo mundo. É a S-C20 num segundo assunto, e a mesma classe do **E196**: manual que CALA a capacidade é invisível para quem lê, porque não há como estranhar o que não está escrito. A `varredura-manuais` passa verde porque confere o MENU, e a devolução não é item de menu~~ | 🔵 | S-C11| ✅ no lote dos manuais (ver S-C96) |
| ~~**S-C46**~~ | **Piso `>=` não obriga a remedir, e a prosa da régua envelhece em silêncio.** A `varredura-portas-sob-tranca` anunciava *"hoje são 48"* e *"8 por CAS"*; medido em 2026-08-13 são **56 portas · 32 TRANCA · 11 CAS · 13 ABERTA**, e **só uma das oito diferenças é da S-C11** — as outras sete entraram em E212, E213, E216 e E221 sem que a régua dissesse uma palavra, porque todas as contagens dela (menos a dívida) são `toBeGreaterThanOrEqual`. Títulos corrigidos e pisos subidos ao medido aqui, e o `replit.md` remedido nos DOIS retratos que carregava (portas e operações do spec, este de antes do E199); a CLASSE fica. É a S30 de 2026-08-05 pelo outro lado — lá era *"trava a lista, não a contagem"*, aqui é *"trava a contagem, mas com folga"*. Quem for fechar troca os `>=` por igualdade onde a conta é do repositório inteiro | 🔵 | S-C11 | ✅ `edfaeba7` · [relatório](execucao/S-C46-S-C55.md) |
| ~~**S-C47**~~ | **A tela e a porta perguntam o teto a contratos diferentes.** `faixa-da-avaria.ts:40` procura o contrato **ATIVO da noiva**; a porta, havendo cobrança viva, confere contra o contrato que **COBRA** o reparo (o mesmo que o `POST /cobrar` escolhe desde o E214). São o mesmo na esmagadora maioria; quando divergem, a tela **oferece o que o 422 recusa** — e o cenário que os separa já está escrito no E214 (a avaria cobrada num contrato em que a peça vale menos). O conserto não é recalcular melhor na tela: é o payload do `Avaria` **dizer** o teto que o servidor usou (`teto`, `conferida`), como o E212 fez com a conta do atraso | 🔵 | S-C11 | ✅ `d63446fb` · [relatório](execucao/S-C47.md) |
| **S-C48** | **A correção da avaria não tem E2E.** `48-avaria-vira-parcela.spec.ts` e `62-avaria-fecha.spec.ts` param na cobrança; o caminho *registrar → cobrar → corrigir → o carnê segue* cruza reserva, avaria, parcela e contrato e não é encenado por spec nenhum. A S-C11 o pregou por **17 testes de API**, que é o que a instrução do worktree permitia medir — a regra 11 pede o E2E completo quando muda o que a trilha grava ou o formato que uma tela lê, e as duas coisas mudaram | 🔵 | S-C11 | aberta |

| ~~**S-C70**~~ | ~~**O estorno avulso devolve R$ 515,00 e deixa R$ 15,00 no caixa.**~~ Medido com sonda no S-C50: estornado o recebimento, sobrava `[{"origem":"MORA","status":"PAGA","recebido":15},{"origem":"PLANO","status":"PREVISTA"}]` — a linha da multa não voltava. **O cancelamento do contrato NÃO tinha o defeito** (seleciona por `contratoId`), e a assimetria é que disse o conserto: o avulso é o único caminho que enxerga uma PARCELA e não o PAGAMENTO | 🟠 | S-C50 | ✅ `e0eb9136` · [relatório](execucao/S-C70-S-C71-S-C90.md) — a linha de MORA morre **CANCELADA**, com os dois carimbos zerados, e **não PREVISTA**: a mora é DERIVADA, e com o principal de volta a PREVISTA a conta da 9ª é refeita do zero — uma linha PREVISTA seria a **mesma multa cobrada duas vezes**. O vínculo é a trilha, a única que o guarda (`MORA_RECEBIDA.parcelaDeOrigemId`, append-only), e nasce `MORA_ESTORNADA`. Abre a **S-C101**: estornar a PRÓPRIA linha de MORA a devolve a PREVISTA e ela passa a render multa e juros **de si mesma** — R$ 15,00 viram R$ 15,45 e crescem todo dia |
| ~~**S-C71**~~ | ~~**A conta da multa é cortada no meio da palavra no carnê.**~~ `contratos.ts:1846` fazia `.slice(0, 200)` sobre uma frase de **209** caracteres, e o corte comia a declaração *"Sem correção monetária — o contrato não nomeia índice"* que o E213 escreveu justamente para a régua não esconder o próprio alcance. **A noiva lê essa linha no portal** | 🟡 | S-C50 | ✅ `e0eb9136` · [relatório](execucao/S-C70-S-C71-S-C90.md) — **e o 200 era palpite**: a nota do S-C50 dizia `varchar(200)` e a coluna é `text`, `character_maximum_length` NULO, sem teto no spec. **Não havia limite a respeitar nem coluna a aumentar** — o corte saiu, e a escolha entre três saídas virou uma. É a regra 22 (*deduzir em vez de ler*) na direção do schema. Piora com o número: R$ 12.500,00 vencidos há 120 dias dão **221** caracteres, e o teste prega os dois casos. Abre a **S-C100**: são **três** `.slice(0, 200)`, e as outras duas cortam **819** e **75** caracteres |
| **S-C72** | **A conciliação não confere que `aoPrincipal + aMora` fecha com o `valorRecebido` do ato.** Um terceiro destino do mesmo pagamento entraria calado — a classe exata da S-C50, um andar acima | 🔵 | S-C50 | aberta |
| **S-C75** | **O critério da S-C46 vale para 1 de 14 varreduras.** Âncora: `varredura-fixture-do-e2e.test.ts:191-203`, onde *specs que escrevem direto no banco* é dívida, a prosa mede **10** e o piso é **8** — as outras treze seguem com piso frouxo | 🔵 | S-C46 | aberta |
| **S-C76** | **`escritasComTabelaDinamica()` nunca se viu achando nada** (`portas-de-escrita.ts:733`). Mesma cegueira da S-C55, na peneira ao lado: `[]` de sonda cega e `[]` de repositório limpo são o mesmo valor, e só o autoteste sintético separa os dois | 🔵 | S-C55 | aberta |
| **S-C77** | **A 29ª transação é a primeira a trancar `avarias`+`parcelas`** (`reservas.ts:2340`), o segundo `FOR UPDATE` é **condicional** (`:2357-2369`) e nenhuma corrida `sm7` exercita o par | 🔵 | S-C46 | aberta |
| **S-C78** | **A peneira de SQL cru é case-insensitive e empurra um achado por TABELA citada** — o mesmo `arquivo:linha` entra duas vezes. Nada disso estava escrito | 🔵 | S-C55 | aberta |
| **S-C79** | **A população é o único número que nada deriva.** A saída sem número mágico é afirmar a DIFERENÇA contra `git ls-files`, não o tamanho | 🔵 | S-C46 | aberta |
| ~~**S-C80**~~ | ~~**O reparo do bloqueio SEM dona entra no carnê de qualquer noiva.**~~ Medido com **201** no S-C47: avaria de R$ 1.500,00 num véu sem dona, cobrada no contrato de uma noiva sem relação com ele — `AVARIA_DE_OUTRA_NOIVA` é `donoDaAvaria && …`, e o nulo atravessava a guarda inteira. Irmã da S-C60 pelo lado do dinheiro, e a população existe: **102 de 227 bloqueios sem dona** no dev | 🟡 | S-C47 | ✅ `b1a21d00` · [relatório](execucao/S-C80-S-C85-S-C86.md) — hoje é **422 `AVARIA_SEM_DONA`**, e a avaria ficava **travada** porque `parcelaId` preenchido bloqueia a remoção. A decisão é **recusar**, e não exigir que quem cobra declare o vínculo (declaração sem prova é assinatura em branco) nem fechar a criação (a S-C60 é decisão de produto e não desfaz os 102 órfãos que já existem). **O argumento que protegia a porta aberta morreu**: a parede media 97% em 2026-07 e mede **0 de 116** hoje — e mede zero também na TELA, que só pergunta o contrato ATIVO quando há dono derivado. O **REGISTRO** da avaria continua entrando: a peça rasgada é um fato, e o que se recusa é o **nascimento do dinheiro** |
| **S-C81** | **O CADASTRO da avaria é a última grafia da conta fora do servidor** — não há payload antes de a linha nascer. O conserto pede o `BloqueioVestido` carregar o aluguel, e ele é schema compartilhado por 10+ operações | 🔵 | S-C47 | aberta |
| **S-C82** | **A listagem paga uma consulta por avaria.** N+1 pequeno e conhecido (0 avarias hoje), anotado para quando alguém listar avarias por LOJA | 🔵 | S-C47 | aberta |
| ~~**S-C85**~~ | ~~**O bloqueio CANCELADO ainda atrasa.**~~ A sobra dizia que o `POST` cobra o bloqueio cancelado — **e não cobra** | 🟡 | S-C32 | ✅ `b1a21d00` · [relatório](execucao/S-C80-S-C85-S-C86.md) — **a sobra estava errada sobre a porta e certa sobre o assunto.** As duas portas que gravam `canceladoEm` num bloqueio de contrato implicam contrato não-ATIVO (o `PATCH /reservas` recusa com 409; o cancelamento derruba os dois juntos), e o `POST` já respondia 422. Quem estava fora do combinado era a **PRÉVIA**: anunciava R$ 3.750,00 (7 dias × R$ 500,00 + a multa de R$ 250,00) sobre um carnê que ninguém pode cobrar — o formato do E213 invertido. Agora responde o mesmo 422. E a decisão ficou **DECLARADA**: quem discrimina é `retiradaDataReal`, porque cancelar não traz o vestido de volta — sem isso o cancelamento seria a **porta dos fundos da 16ª**, e os R$ 12.000,00 do extravio sumiriam com um clique |
| ~~**S-C86**~~ | ~~**A peça atrasada SEM contrato é invisível para a 16ª.**~~ Fila e prévia varrem `contratos → contrato_bloqueios`, e `disponibilidade.ts` pinta `ATRASO_DEVOLUCAO` do mesmo jeito: **o acervo mostrava a peça vermelha e a régua do dinheiro não a enxergava** | 🟡 | S-C32 | ✅ `b1a21d00` · [relatório](execucao/S-C80-S-C85-S-C86.md) — a cobrança **fica presa ao contrato** (a 16ª cobra sobre o aluguel de cada peça, e aluguel só existe em `contrato_itens`), e o conserto é a fila **DIZER o que não olha**: `FilaDeAtrasos.semContrato` traz a peça nomeada, com a noiva e os dias, entra na contagem de peças e **não soma um centavo**. Caem nela as duas populações — a que nunca virou contrato e a que ficou solta quando o contrato caiu, que é a S-C85 pelo outro lado. O sino passou a acender sozinho com uma órfã |
| **S-C87** | **A fila avisa e não age** — cobrar é sempre um ida-e-volta até a ficha. De propósito, mas com três peças fora são três idas | 🔵 | S-C32 | aberta |
| ~~**S-C88**~~ | ~~**Nenhum manual sabe que a 16ª existe** — `git grep -in "16ª" docs/manuais/` = zero, e a `varredura-manuais` passa verde porque confere o MENU. O achado do E196 na mesma direção~~ | 🔵 | S-C32| ✅ no lote dos manuais (ver S-C96) |
| **S-C89** | **A fila faz 1 consulta larga + 2 por contrato atrasado, e o sino as refaz a cada 5 min em toda tela.** Nenhuma régua trava esse número | 🔵 | S-C32 | aberta |
| ~~**S-C90**~~ | ~~**O § único fica sem dono no `PATCH`.**~~ `POST /contratos` e `gerar-plano` conferem o carnê contra a retirada (E218); a terceira porta, que desde o E224 **MOVE a retirada**, não conferia nada | 🟡 | E224 | ✅ `e0eb9136` · [relatório](execucao/S-C70-S-C71-S-C90.md) — contrato de R$ 2.000,00 vencendo 10/07 e 20/08: mover a retirada para 04/09 põe o limite em 15/08, e **R$ 1.000,00 — metade da venda — só entrariam 5 dias depois de a peça sair pela porta**. O `POST` recusava com 422; o `PATCH` gravava 200. A guarda vale **só** quando a retirada está no corpo, **só** para o CARNÊ (origem PLANO — avaria, atraso e mora nascem depois da retirada) e **só** para o que está EM ABERTO, e o campo da recusa é `dataRetirada` |
| ~~**S-C91**~~ | ~~**A ficha da noiva mostra a reserva e não a locação**~~ — quem atende olha a ficha, não o contrato | 🟡 | E224 | ✅ `68047846` · [relatório](execucao/S-C91.md) — **conserto inteiramente de tela**: o dado já chegava (`Contrato.dataRetirada`/`dataDevolucao` no spec, `findMany` com colunas inteiras), sem uma linha de spec, codegen ou migração. As duas datas entram no card **"O casamento"**, que é o card do QUANDO, em `instanteCurto` — a locação é INSTANTE no relógio da loja, enquanto o aviso de reserva três parágrafos acima segue em `diaMesAno` porque ali a data é dia de NEGÓCIO em UTC, **e a varredura prega as duas réguas convivendo**. Campo vazio não vira linha vazia, e o número manda: **733 contratos, 1 com retirada, 0 com devolução, 311 ATIVOS, 1 ativo com alguma data** — 310 das 311 fichas não têm o que mostrar. Devolve `null` também para CANCELADO (a peça voltou ao mercado no E111), e **não cala a metade que falta**: com retirada e sem devolução a ficha diz *"A informar"*, porque é a devolução que a multa da 10ª cobra. O manual da vendedora listava quatro campos onde há seis — o defeito invisível do E196 |
| ~~**S-C92**~~ | ~~**O portal da noiva entrega `retiradaPrevista` e NÃO a devolução** — que é a data que a multa da 10ª cobra~~ | 🔵 | E224 | ✅ E230 `f88802d9` — `devolucaoPrevista`/`devolucaoFeitaEm` no `VestidoDaNoiva`, simétricos à retirada; sem data combinada o portal SILENCIA. Verde falso medido antes do vermelho: campo ausente vem `undefined` e `.not.toBeNull()` passa — os asserts viraram `toBeTruthy` |
| **S-C93** | **`min`/`max` do `datetime-local` são absolutos e a faixa da 4ª muda com o dia** (sábado fecha antes). A solução é um seletor que conheça o expediente, não o atributo | 🔵 | E224 | aberta |
| **S-C94** | **`moscow_base` segue com 0 contratos**: o gesto do E224 nasce sem dívida de migração **e sem um caso real para conferir a régua** | 🔵 | E224 | aberta |
| ~~**S-C95**~~ | ~~**A `varredura-manuais-prazos` só sabe TEMPO** (5 constantes em ms). Os números das cláusulas são dinheiro, multiplicador e percentual — o manual pode escrever R$ 300,00 de piso e ela passa verde~~ | 🔵 | S-C45| ✅ `d5733a96` — a régua saiu de **5 réguas de tempo para 18**, com as 13 do contrato. Duas delas **não existiam como constante**: os 60% da 11ª eram `* 0.4` solto em `rescisao.ts:191` e os 30 dias da 13ª §3º um `30` cru em `contratos.ts:1847` — **número sem nome é invisível para a régua**, que lê `const NOME = <expr>;` da fonte. E a regra de conversão estava invertida para as novas: *"termina em `_DIAS` fica, o resto vira tempo"* transformaria `MULTA_DE_ATRASO = 250` em **0,0000029 dias**, e a divergência ficaria invisível porque ninguém escreveria esse número. **Régua que converte o que não devia não reprova: ela compara contra um número que ninguém escreveria** |
| ~~**S-C96**~~ | ~~**Os manuais não conhecem o contrato de papel.** Não é a avaria que falta: nos cinco documentos são **0 "avaria", 0 "multa", 0 "juros"**, "cláusula" aparece 2 vezes e as duas são da 7ª. **Oito épicos executados (E211–E222) e nenhum chegou aos manuais** — inclusive a mora da 9ª, que a noiva lê no portal dela. **Fecha S-C45 e S-C20 junto, depois da onda C**~~ | 🟡 | S-C45| ✅ `1cf009c4` (vendedora) · `1ea146d8` (noiva) · `0aecd062` (proprietário) · `9165ea2a` (recepção e costureira) — **quatro agentes em paralelo, um por audiência, zero conflito.** Os cinco documentos saíram de **0 "avaria", 0 "multa", 0 "juros", 0 "rescisão", 0 "exclusiva"** para **38 · 88 · 38 · 17 · 32**, e as células pregadas por régua foram de **9 para 69**. **Fecha S-C20, S-C45, S-C88, S-C97 e S-C113 junto.** E os agentes acharam DOIS erros de quem encomendou, os dois de deduzir em vez de ler: o briefing mandava dizer que **a costureira registra a avaria com foto** — ela tem `vestidos: SO_VER` (`configuracao-inicial.ts:159`) e **o formulário não existe na tela dela**; e mandava a recepção responder *"quando devolvo o vestido?"* — ela **não vê a data**, porque a S-C91 deriva de `contratosDaNoiva`, que para ela é `[]`. Terceira e quarta ocorrências da lição da entrega 4, agora do lado de quem escreve a tarefa |
| ~~**S-C97**~~ | ~~**O manual da vendedora termina em "9 · O contrato"** — falta o passo 10, *"A peça sai e volta"*~~ | 🔵 | S-C45| ✅ no lote dos manuais (ver S-C96) |
| **S-C98** | **A cobrança do reparo vence em 7 dias e a tela nunca escolhe o prazo** (`reservas.ts:1893` × `[bloqueioId].tsx:428-432`) | 🔵 | S-C45 | aberta |
| **S-C99** | **O botão de corrigir a avaria não tem nome na tela**, só `aria-label` (`[bloqueioId].tsx:1213-1222`) | 🔵 | S-C45 | aberta |
| ~~**S-C100**~~ | ~~**A S-C71 apontava uma `.slice(0, 200)`; são TRÊS.** As outras duas estão em `reservas.ts` e cortam mais: `:1994` grava `Reparo de avaria — ${descricao}` com **1019** caracteres (**819 cortados**, porque a descrição da avaria tem `maxLength: 1000` no spec) e `:2454` grava o atraso com **275** para 3 peças (**75 cortados**). O segundo **cresce com o número de peças**, então o corte engole primeiro justamente *"Total R$ 1.340,00."*, que é a linha que a noiva confere no portal. Zero dívida no banco (a maior descrição tem 52 caracteres): é mecanismo, e o conserto é o mesmo — a coluna é `text`~~ | 🟡 | S-C71| ✅ `33b54bdc` · [relatório](execucao/S-C100.md) — **e o plano errou o número de sítios e o vermelho.** Eram **DOIS** `.slice(0, 200)` vivos, não três (o de `contratos.ts:2083` já era só comentário desde a S-C71), e os dois andaram de linha (`reservas.ts:2048` e `:2648`). **Tirar o corte NÃO reprovava o `e212-...test.ts:182`**, como o plano afirmou: a fixture de lá é de UMA peça e a frase dá **147 caracteres** — o corte sobre 147 é inócuo, e o assert pregava o defeito **na letra e não no efeito**, passando nas duas versões do código. O vermelho teve de ser CONSTRUÍDO (três peças): `Expected: "…multa de atraso (cláusula 16ª §1º): R$ 250,00. Total R$ 1.450,00."` contra `Received: "…= R$ 66,66; multa"` — **60 caracteres perdidos, e o que some é o Total que a noiva confere no portal**; o reparo perde até **819**. **A régua foi RECUSADA com número**: dos **75 `.slice(` de produção**, só dois truncam texto a caminho de coluna, e o discriminante não é o tipo dela — `diaLocal(x).slice(0, 7)` também vai para coluna `text` e é **projeção** (11 dos 75 têm essa forma). Dívida no banco: zero, sem migração |
| ~~**S-C101**~~ | ~~**Estornar a PRÓPRIA linha de MORA a devolve a PREVISTA, e ela passa a render multa e juros de si mesma.**~~ Medido com sonda: 200, e a porta devolvia `{"origem":"MORA","status":"PREVISTA","mora":{"multa":0.3,"juros":0.15,"total":15.45}}` — **R$ 15,00 viravam R$ 15,45 e cresciam todo dia**, com a principal ainda PAGA e a cobrança órfã no carnê | 🟡 | S-C70 | ✅ `5dd98230` · `moraDe` (`lib/mora-da-parcela.ts:35`) passou a recusar também `origem === "MORA"`, no molde da CANCELADA — a linha da multa não pode render multa de si mesma. Vermelho reproduzido antes do conserto (`sc101-estorno-da-propria-mora-api.test.ts`): estornar a linha de MORA isoladamente e reler na fila devolvia `{multa:0.3,juros:0.15,total:15.45}`; depois, `null`. API 1618 (222 arquivos), verde |
| **S-C102** | **A trilha da mora guarda os números e NÃO guarda a frase.** `MORA_RECEBIDA` grava `valor`, `diasDeAtraso`, `multa` e `juros` (`contratos.ts:1956-1963`) e nunca a `explicacao` — ao contrário do que a nota do S-C50 supunha. Hoje não custa nada, porque a descrição da parcela a carrega inteira desde a S-C71; mas **descrição é coluna editável e trilha é append-only**, e é a trilha que existe para responder *"por que se cobrou isto?"* depois do fato. Uma linha no `detalhe` | 🔵 | S-C70 | aberta |
| ~~**S-C110**~~ | ~~**A peça que saiu e não voltou SOME da disponibilidade quando o bloqueio é cancelado.** `buscarBloqueiosAtivos` (`lib/disponibilidade.ts:437`) filtra `cancelado_em IS NULL`, e a régua física só roda sobre o que ele devolve: cancelar o contrato solta a peça ao acervo **enquanto ela está na casa da noiva** — outra noiva a reserva para a mesma data, e a dupla promessa só aparece na retirada. Classe da S-M7 e da S-M24, pelo caminho do cancelamento de CONTRATO. O conserto provável é um predicado (cancelado **com retirada e sem devolução** continua ocupando) e **muda o que dez portas respondem** — escopo próprio com E2E inteiro. População hoje: 0 cancelados e 0 com retirada nos dois bancos~~ | 🟡 | S-C86 | ✅ E225 `68fa7239` — ocupar ganhou a segunda forma ("na rua": retirada real sem devolução, cancelado ou não — o predicado da S-C85 aplicado à disponibilidade), num lugar só, e as dez portas herdaram. Cancelado, só a ocupação FÍSICA sobrevive: a PROVA de contrato morto não agenda nada. Vermelho literal: a noiva B levava **201** sobre a peça na casa da A, e o calendário dizia DISPONIVEL. A devolução se registra no bloqueio cancelado e liberta. Abre S-C233 🔵 (a lavagem da peça devolvida de contrato morto) |
| ~~**S-C111**~~ | ~~**A varredura das órfãs é por LOJA, sem índice e sem teto, e o sino a refaz a cada 5 min.** `pecasForaSemContrato` consulta `bloqueio_vestidos` da loja inteira por `retirada_data_real IS NOT NULL`; numa loja com legado importado (as 132 peças de `moscow_base`) toda peça já retirada e nunca devolvida entraria de uma vez, e o sino diria *"347 peças fora do prazo"*. Irmã da **S-C89**, e a mesma resposta serve às duas: contagem pregada ou paginação~~ | 🔵 | S-C86 | ✅ E231 `56ce9363` — índice parcial `loja_na_rua_idx` (aplicado no `heliumdb`, DDL em `docs/migracoes/2026-08-14-e231-indice-orfas.sql`) e o teto DITO: `orderBy casamento asc · limit 200` — o corte deixa de fora as MENOS graves, e o comentário diz por quê. A S-C89 (o custo do sino) segue aberta |
| ~~**S-C112**~~ | ~~**O 422 `AVARIA_SEM_DONA` ensina o caminho, e a tela nunca o mostra.** O corpo diz *"Ligue a reserva à noiva antes de cobrar"* e ninguém lê: `reservas/[bloqueioId].tsx` não desenha o botão sem dono derivado (`enabled: !!donoDaReserva`). Quem abre um véu órfão com avaria vê o dano, não vê saída e **não vê o motivo** — a classe do E166: um aviso onde o botão não está, dizendo por que ele não está~~ | 🔵 | S-C80 | ✅ E230 `f88802d9` — a frase do 422 aparece no lugar do botão que não nasce (`motivo-nao-cobra-<id>`): quem abre o véu órfão com avaria vê o dano E a saída |
| ~~**S-C113**~~ | ~~**Nenhum manual sabe que a peça pode estar fora sem contrato.** `git grep -in "16ª" docs/manuais/` segue em **zero** — é a **S-C88** com dois fatos novos que ela não cobre: a fila agora tem um lado sem preço (`semContrato`) e a cobrança do reparo agora **recusa** a peça órfã~~ | 🔵 | S-C86| ✅ no lote dos manuais (ver S-C96) |
| ~~**S-C114**~~ | ~~**O `semContrato` não distingue "nunca teve contrato" de "o contrato caiu".** A ação certa é diferente — a primeira é gesto de balcão que não virou venda; a segunda é venda desfeita com a peça na rua, e pode ter parcela de atraso **já cobrada** num carnê morto. A tela diz *"sem contrato ativo"* para as duas. Pede campo (`contratoCanceladoId`) e frase; ficou fora porque a população é zero e o formato já estava mudando~~ | 🔵 | S-C86 | ✅ E231 `56ce9363` — `contratoCanceladoId` no payload (o mais recente dos cancelados) e a fila diz qual é o caso: *"a venda foi desfeita — ver o contrato"* com link, ou *"nunca virou contrato"* |
| ~~**S-C115**~~ | ~~**Desfazer a retirada deixa o atraso COBRADO órfão.** `PATCH /bloqueios` aceita `retiradaDataReal: null` como *"desfaça"* (E61) sem guarda contra desfazê-la depois de o atraso virar parcela: `pecasAtrasadasDoContrato` para de contar, a prévia responde `devida: false` **e `jaCobrada: true` com o `parcelaId`**, porque `contratos.atraso_parcela_id` continua de pé — a ficha mostra "Cobrado" sobre uma conta que ela mesma diz não existir, com os R$ 3.750,00 vivos no carnê. Irmã do que o E115/S5 fechou para o recebimento~~ | 🔵 | S-C85 | ✅ E231 `56ce9363` — 422 `ATRASO_JA_COBRADO`, a irmã das duas guardas que moram três linhas acima na mesma rota, com a saída na frase; a régua da vida é a MESMA da recobrança (`cobrancaViva`), e parcela CANCELADA devolve o desfazer |
| ~~**S-C120**~~ | ~~**A ficha diz "Nenhum contrato ainda." para quem não PODE ver contrato.** `noivas/[leadId]/index.tsx:834-837` trata `isLoading` e depois `length === 0`, e **403 cai no segundo ramo** (`data` fica `undefined`, a lista vira `[]`). A **Recepção** tem `contratos: NADA` desde o E172 e é justamente quem atende o telefone: a ficha afirma a ela que a noiva **não tem contrato** quando tem um ativo. O card Orçamentos tem a mesma forma (`:803-806`), e lá não aparece só porque a Recepção tem `SO_VER`. O conserto é o idioma que a ficha já tem: distinguir *"não há"* de *"você não pode ver"*~~ | 🟡 | S-C91| ✅ `202d4816` · [relatório](execucao/S-C120.md) — **não são duas telas: é uma tela que fala TRÊS vezes**, e a terceira é a pior. A mesma lista silenciada alimenta o banner do próximo passo (`index.tsx:507`), que mandava **"Fechar o contrato — ela já disse sim"**, em botão, para a Recepção, sobre um contrato **já fechado** que ela não pode fechar. `temContratoAtivo` virou tri-estado, no idioma que `temVisitaFutura` e `temAceiteSemContrato` já falavam. E o card de Orçamentos **não estava só escondido pela permissão**: o `?? []` não pergunta permissão nenhuma, então um 500 produz a mesma frase falsa para a Vendedora, que tem `orcamentos: TUDO`. Vermelho literal: `expected 'pronto' to be 'sem-permissao'` e `expected { titulo: 'Fechar o contrato', …(3) } to be null` — **6 testes em 3 arquivos**. População: **739 de 1426 fichas (51,8%) mentiam para a Recepção**, 311 sobre contrato vivo; **o banner tem população ZERO**, e é o achado mais grave dos dois — *população mede quanto o defeito já custou, não quanto ele custa quando acontece*. A distinção 403-de-perfil × 403-de-loja-errada **não é possível pelo status e não precisa ser**: nos dois casos a frase honesta é a mesma |
| ~~**S-C121**~~ | ~~**A ficha promete uma retirada que já aconteceu.** As duas linhas novas saem de `contratos.dataRetirada`/`dataDevolucao`, que é o **combinado**; a peça que já SAIU tem `bloqueio_vestidos.retiradaDataReal`, e é ela que o portal da noiva mostra como `retiradaFeitaEm` ao lado da prevista (`visao-noiva.ts:276-277`). A ficha da LOJA não tem a real: segue dizendo *"Retirada 12/05/2027 10:30"* depois de o vestido ter saído. É trabalho de **payload**, não só de tela~~ | 🔵 | S-C91 | ✅ E231 `56ce9363` — o recorte do E229 aprendeu as reais, **e a régua das chaves enumeradas mordeu como devia** (campo novo só entra com a decisão escrita, e ela está). A união N:N+legado virou `bloqueioIdsDoContrato` (regra 26: segundo leitor, extrai). A ficha diz *"feita em DD/MM"* |
| ~~**S-C130**~~ | ~~**`PARES` da `enums-do-contrato` é lista curada à mão, e o E215 provou.** A régua prega os `z.enum` dos formulários contra o contrato da API enumerando `PARES` — o que alguém lembrou de escrever, **não** os `z.enum` do repositório. O `estadoCivil` do E215, um enum de **seis valores** num formulário usado em toda noiva, passou verde por não ter sido olhado; só entrou porque quem escreveu o épico foi conferir. É a **S-C33** e a **S-C55** numa terceira lista, e o sexto enum nascerá invisível igual. O conserto é derivar `PARES` do fonte (varrer `pages/**` por `z.enum([`) em vez de mantê-la. Custo da divergência aqui é maior que nos quatro pares antigos: um valor a menos e a noiva DIVORCIADA não é qualificável, então o contrato dela **não fecha** — e a mensagem seria sobre campo vazio, não sobre opção que falta no select~~ | 🟡 | E215| ✅ `36a71b67` · [relatório](execucao/S-C130.md) — **e o plano errou para MENOS**: `pages/**` descreve onde os cinco estão, não onde o sexto pode nascer. A varredura entregue cobre **`src/` inteiro menos os testes — 193 arquivos**, contra 73 de `pages`, e achou os mesmos 5 — o que acrescenta um fato que ninguém tinha: **não há `z.enum` de formulário fora de `pages/` hoje**. **Contagem de pares 5 → 5, idêntica**, que era a régua de aceitação. Duas peneiras declaradas (população versionada sem testes · grafia `campo: z.enum([` contígua), e **a armadilha do `:47` cai pelas duas** — o `git grep` que devolvia 6 passa a devolver 24, e as 18 novas estão todas dentro do arquivo de teste. **Exceção nomeada: NENHUMA**, porque nenhum defeito medido pediu uma (S-C56). Vermelho do sexto enum sintético: `× hooks/zz-formulario-sintetico.ts#formaPagamento — SEM PAR` · `Tests 3 failed | 16 passed (19)`, contra **`Tests 7 passed (7)` da régua antiga sobre a mesma árvore** — sete verdes sobre um enum que a lista curada não via. Sintético desfeito |
| ~~**S-C131**~~ | ~~**O expurgo da LGPD segue `set({…})` curado à mão** — o que o E215 mudou é que agora há régua cobrando (`varredura-expurgo-lgpd`), não que a lista tenha deixado de ser lista. Derivá-lo do schema é a mesma conversa da S-C33, e aqui a direção do erro custa PROCESSO: coluna pessoal fora da lista **sobrevive à anonimização**~~ | 🔵 | E215 | ✅ por DECISÃO (14/08, na recomendação): **não derivar agora** — a régua do E215 já cobra a lista, e a derivação é a conversa da S-C33, adiada com registro. Se a régua falhar uma vez, a decisão reabre |
| **S-C132** | **O PDF do contrato ainda não imprime a qualificação.** Os treze dados estão congelados em `contratos` desde o E215 e o `contrato-pdf.ts` desenha só `dado("CPF", …)`. Não é defeito: imprimir as 21 cláusulas com a qualificação da locatária é o **E220**, que segue travado em D4 e D7. Fica contável para o E220 não redescobrir que os dados já existem | 🔵 | E215 | aberta |
| ~~**S-C133**~~ | ~~**Os 735 contratos já fechados têm a qualificação VAZIA, e ninguém decidiu o que fazer com eles.** O E215 não os toca de propósito — eles nasceram sob outra regra, e reescrever o passado seria dizer que a noiva assinou o que não assinou. Mas se a loja reimprimir um contrato antigo, o papel sai sem quem assina. É decisão da dona: reimprimir puxando da ficha de hoje (o dado certo, no papel errado) ou sair em branco como sempre saiu~~ | 🔵 | E215 | ✅ por DECISÃO (14/08, na recomendação): os contratos antigos **ficam como estão** — nasceram sob outra regra, e a reimpressão sai em branco como sempre saiu. Reescrever o passado diria que a noiva assinou o que não assinou |
| ~~**S-C140**~~ | ~~**O diálogo "Cancelar contrato" não mostra o que a cláusula manda reter/devolver.** `contratos/[id].tsx:1022` segue com a escolha manual "A noiva perdeu o sinal" / "Devolvi o valor — estorna [tudo]", exatamente como antes do E217 — e a segunda opção estorna 100% do que entrou, contradizendo a 8ª §2º (a reserva nunca volta). A conta certa já chega pronta em `POST /cancelar`'s `rescisao` (retido/devolvido por cláusula), e o diálogo não a lê: é preview ANTES do clique que falta, no molde do E211/E216/E218~~ | 🟡 | E217| ✅ `f516b29f` · [relatório](execucao/S-C140.md) — **o conserto não era de tela, e a medição provou**: `ItemDaRescisao` exige `exclusivaDePrimeiroAluguel` e `ContratoItem` não o carrega, então recomputar no front seria **adivinhar a metade cara da conta**. O `GET /contratos/:id` passou a preencher `Contrato.rescisao` (campo que existia desde o E217 e que só a resposta do `POST /cancelar` preenchia), com `hoje` injetado — e **fazia 2 queries no handler e continua fazendo 2**: as duas metades do predicado da 12ª entraram na consulta que já existia. A leitura ingênua foi medida e recusada (`expected 3 to be 2`). Achado fora do plano: **a contagem da 12ª precisa de `c_outros.id <> $`**, que o `POST /cancelar` não precisa porque lá ela roda DEPOIS do `UPDATE` para CANCELADO. Vermelho: `TypeError: Cannot read properties of undefined (reading 'retencaoTotal')` · `Tests 5 failed | 5 passed (10)`. **Fecha junto a S-C150 🟠** |
| ~~**S-C150**~~ | ~~**O E217 mudou o spec e não re-rodou o codegen, e o `GET /financeiro/contas-pagar` responde 500.**~~ `openapi.yaml:7572` e `:7603` listam `DEVOLUCAO` no `tipo` da conta a pagar; o zod gerado tinha **quatro valores, sem ele** — então a conta que a própria 13ª §3º cria fazia a listagem inteira responder `RESPOSTA_FORA_DO_CONTRATO`. Não é a linha nova que cai: é a tela toda. **Conferido no `main` antes do conserto**, e a população dizia que ainda não tinha disparado: **1254 linhas em `contas_pagar`, ZERO `DEVOLUCAO`** (708 DESPESA, 332 FORNECEDOR, 214 COMISSAO) — armado desde ontem, dispara no primeiro cancelamento com devolução. O `_cobreTodosOsTipos` de `dre.ts` existe exatamente para pegar isso e **não pegou**: *guarda que depende do codegen só protege depois de o codegen rodar* | 🟠 | S-C140 | ✅ `f516b29f` (no mesmo commit) · [relatório](execucao/S-C140.md) |
| ~~**S-C151**~~ | ~~**A cláusula 13ª existe na API e nenhuma tela a alcança.** O E217 criou `iniciativa: LOCATARIA \| LOJA` — quando a LOJA cancela, ela devolve tudo —, e `iniciativa` aparece **0 vezes em `pages/` e 0 em `e2e/`**: só se chega nela pela API. É o formato do E222 (o campo existia e nenhuma tela o oferecia) e do E197, pela terceira vez nesta trilha~~ | 🟡 | S-C140 | ✅ E227 `19296ca1` — o diálogo de cancelar pergunta "quem está rescindindo?": com a loja, o painel vira a 13ª (devolve tudo, 30 dias do §3º), o aviso de estorno-contra-cláusula não acusa quem obedece, e a trilha grava `iniciativa: LOJA · retenção 0 · devolução integral` (spec 63) |
| ~~**S-C152**~~ | ~~**Nada cobra que `generated/` esteja em dia com o `openapi.yaml`.** A S-C150 nasceu e viveu um dia inteiro porque o codegen é um gesto humano que ninguém prega. Uma régua de ~10 linhas (re-rodar o codegen e exigir árvore limpa) a teria pego no dia em que nasceu — e é a mesma classe da S-C33/S-C55/S-C130: o que se cura à mão envelhece calado~~ | 🔵 | S-C140 | ✅ — `varredura-codegen-em-dia.test.ts`: fotografa diff+status dos dois `generated/`, re-roda o orval (~7 s, determinístico como a S-D44 promete — 0 arquivos sobre árvore limpa) e exige a mesma fotografia. NÃO exige árvore limpa: quem está no meio de um épico com spec e gerado EM DIA passa; quem esqueceu o codegen reprova e sai com a árvore já corrigida. Vermelho encenado no formato exato da S-C150 (enum novo no spec, codegen não rodado): a régua acusou nomeando o valor plantado |
| ~~**S-C153**~~ | ~~**A `varredura-schemas-aninhados` mede a FORMA da escrita, e isso não está nos pontos cegos dela.** Ela só desce em literal cujo valor começa em `{`, então campo escrito por extenso no handler passa invisível. Custou uma medição no E213 e outra aqui~~ | 🔵 | S-C140 | ✅ por DECISÃO (14/08, na recomendação): **manter, dito** — o ponto cego da forma da escrita está declarado na própria varredura e o preço já foi pago duas vezes (E213, S-C140); um parser de AST não compra o que custaria. A declaração no arquivo é o registro |
| **S-C160** | **"Nenhuma avaria registrada — o vestido voltou como saiu."** `reservas/[bloqueioId].tsx:1114` afirma isso sobre `(avarias.data ?? [])`. **Sem eixo de permissão** (a listagem vive sob o mesmo `vestidos` da página), mas um 500 produz a frase — e é a que a dona lê antes de decidir **não cobrar** avaria | 🔵 | S-C120 | aberta |
| **S-C161** | **O E121 fechou o defeito irmão com uma VARREDURA; a S-C120 não deixou a equivalente.** Nada impede o próximo card de nascer com `?? []` e frase de vazio. É a lição da S-C30 (grep acha os de hoje, régua impede o de amanhã) na direção das telas | 🔵 | S-C120 | aberta |
| **S-C162** | **`contratos/[id].tsx:870` e `:948` dizem "Nenhuma parcela registrada." pelo mesmo caminho.** A origem da lista não foi medida, por isso 🔵 e não 🟡 | 🔵 | S-C120 | aberta |
| **S-C163** | **`locacaoDaNoiva` e `reajustePrevisto` leem a mesma lista e ficam em SILÊNCIO**, não em mentira. Registrado porque uma frase de fallback ali nasceria falsa | 🔵 | S-C120 | aberta |
| ~~**S-C170**~~ | ~~**O PDF do contrato desenha a cobrança extra numa linha só, e ela sai da página.** `contrato-pdf.ts:109` e `:120` chamam `add()` sem passar pelo `quebrarTexto` que existe no mesmo arquivo e é chamado num lugar só (`:134`); `LARGURA_TEXTO` é **92**. Medido com sonda sobre `gerarContratoPdf`: a linha de `MORA` sai com **240 caracteres** e a de `ATRASO_DEVOLUCAO` com **294**. **É anterior ao conserto da S-C100** — a MORA de 240 já sai da página no `main` desde a S-C71, que tirou o corte sem que ninguém olhasse o papel. Mexe no documento que a noiva assina e precisa dos golden do `e165`: **é épico próprio**~~ | 🟡 | S-C100 | ✅ `a2822b82` — **a quebra mudou de DONO**: saiu dos call-sites e entrou no `emitir()` do paginador (`pdf-desenhista.ts`), por onde toda linha de todo papel passa — a mesma mudança de dono do E221. Régua de EFEITO: lê as linhas que o PDF **desenha** (`(...) Tj`), e um `add()` novo sem quebra reprova no dia em que nascer. Vermelho literal: `linha de 339 … expected 339 to be less than or equal to 92`. Golden do e165 e `contrato-pdf.test.ts` verdes sem uma linha mudada |
| ~~**S-C171**~~ | ~~**`.slice(0, 500)` sobre `backup_log.erro`** (`lib/backup.ts:67` e `scripts/restore-drill.ts:308`), coluna `text` sem teto: o 500 é o mesmo palpite que o 200 era. Aqui o veredito pode muito bem ser **manter** — o que falta é ele estar escrito~~ | 🔵 | S-C100 | ✅ por DECISÃO (14/08, na recomendação): **manter o corte**, e o veredito agora está ESCRITO nos dois sítios (`lib/backup.ts` e `restore-drill.ts`) — é log de diagnóstico, os primeiros 500 caracteres dizem a causa, e difere do 200 da S-C100, que cortava descrição que o carnê imprime |
| ~~**S-C180**~~ | ~~**A régua prega o `z.enum` e não prega o que a tela OFERECE.** É a lista renderizada que decide o que a vendedora escolhe, e o preço que a própria S-C130 descreve (a noiva DIVORCIADA que não fecha contrato) mora desse lado. Quatro sítios desenham com `<SelectItem value>` e batem hoje; o quinto tem **segunda cópia à mão 406 linhas abaixo** — `atendimentos/novo.tsx:82` (`z.enum(["ATENDIMENTO","PROVA"])`) contra `:488` (`(["ATENDIMENTO", "PROVA"] as const).map`), e uma terceira em `:126`. Divergência hoje zero nas três; no dia em que `AtendimentoTipo` ganhar um terceiro valor, a régua cobra a linha 82 e os botões da 488 continuam dois, **verdes**~~ | 🟡 | S-C130 | ✅ `e176746d` — as duas metades: a tela DERIVA a oferta do schema (`agendarSchema.innerType().shape.tipo.options` — os botões e o prefill saem dele, três grafias viram uma) e a varredura reprova a segunda cópia (`listasConstNoTexto`: num arquivo com `campo: z.enum`, nenhuma lista literal `as const` repete o conjunto — **a cópia idêntica de hoje é a divergência de amanhã, e só a idêntica é detectável**). Vermelho reproduzido por `git stash push -- <arquivo>` contra a cópia antiga, com a denúncia nomeando arquivo, campo e conserto |
| **S-C181** | **O contrato tem 3 a 4 enums paralelos por campo, e nada prega que digam o mesmo.** A ponte confere `origem` contra `LeadOrigem` (SAÍDA) enquanto o formulário manda `LeadInput`. As famílias coincidem hoje, **menos uma**: `CaptacaoLeadInputOrigem` tem **3 valores, sem `LOJA`** — prova de que o spec sabe estreitar enum de ENTRADA. Quando isso acontecer num campo de formulário, a varredura aprova uma tela que oferece valor que o `POST` recusa | 🔵 | S-C130 | aberta |
| **S-C182** | **O arquivo sintético que prova uma régua nunca está documentado.** Terceira vez na trilha (S-C33, S-C46, S-C130) que um épico cria arquivo, roda `git add`, mede e desfaz — e **o `git add` é obrigatório** porque a população vem do `git ls-files`. Esquecê-lo faz a encenação passar em silêncio: verde por não ter olhado, de novo | 🔵 | S-C130 | aberta |
| ~~**S-C190**~~ | ~~**O carnê do contrato não mostra a mora, e é a única tela de dinheiro que a Vendedora abre.** Ela tem `financeiro: NADA` (`configuracao-inicial.ts:147`), então a fila de cobrança e o bloco de Mensagens não existem para ela. O dado CHEGA (`contratos.ts:179`, `Parcela.mora` no spec) e a tela imprime só `valorPrevisto` (`contratos/[id].tsx:748`); o diálogo pré-preenche `saldoAberto`. Numa parcela de R$ 500,00 vencida há 30 dias: o portal da noiva diz **R$ 515,00**, a porta ACEITA R$ 515,00, e a tela dela oferece **R$ 500,00** — os R$ 15,00 da cláusula ficam no chão. **É o E213 invertido**: lá a porta recusava o que as leituras mostravam; aqui a leitura esconde o que a porta aceita~~ | 🟡 | S-C96 | ✅ E226 `6d1cf08a` — e a sobra estava errada sobre si mesma: **o dado NÃO chegava**. A âncora `contratos.ts:179` é o helper das rotas de PARCELA; o `GET /contratos/:id` montava as parcelas com `with: { parcelas: true }` cru e `Parcela.mora` é `optional`, então nada reprovava. Vermelho medido: `expected null to not be null` no carnê. O conserto tem as três camadas (porta, grafia única em `mora-na-tela.ts`, tela) e alcançou a carteira do financeiro (`receber.tsx`), que tinha o mesmo defeito |
| ~~**S-C191**~~ | ~~**O perdão da mora existe na API e em nenhuma tela.** `PerdoarMora` aparece **0 vezes em `pages/` e 0 em `e2e/`**. O único gesto notável da 9ª — abrir mão da multa — não tem botão. Formato do E222 e da S-C151, terceiro sítio da mesma trilha~~ | 🔵 | S-C96| **FUNDIDA na S-C210** — é o MESMO defeito, achado por dois agentes do lote dos manuais por lados opostos: o da vendedora pelo carnê, o do proprietário pelas rotas. Conferido: `PerdoarMora` tem **0 usos em `pages/`** e o selo do perdão já é desenhado. Fica aqui como registro de que a duplicata existiu — é o formato que o lote das seis já tinha produzido (o mesmo achado com três números), e que cabe ao integrador fundir |
| ~~**S-C200**~~ | ~~**O portal soma o principal em cima e o total com mora embaixo, na MESMA tela.** *"Falta pagar"* e *"Próxima parcela"* saem de `abertoEmCentavos` (`portal.ts:250,252`) e a linha da parcela mostra o total com mora (`noiva-portal.tsx:703`): **R$ 500,00 em cima, R$ 515,00 embaixo**, para a mesma dívida. É a noiva conferindo dois números da loja e escolhendo em qual acreditar~~ | 🟡 | S-C96 | ✅ E226 `6d1cf08a` — vermelho literal `expected 500 to be 515` em `faltaPagar` e `proximaValor`. O resumo soma `moraDe(p)?.total ?? saldoAberto(p)` por parcela aberta: o `??` faz o perdão descer sozinho, sem régua segunda |
| ~~**S-C201**~~ | ~~**O portal não mostra a data de DEVOLUÇÃO.** `VestidoDaNoiva` (`visao-noiva.ts:184-191`) traz retirada prevista e feita, e nenhum campo de devolução — embora `contratos.dataDevolucao` saia no PDF (`contrato-pdf.ts:129`). É a data a partir da qual a conta da 16ª começa a correr, e é a única que a noiva não vê. Irmã da S-C92~~ | 🔵 | S-C96| **FUNDIDA na S-C92** — a mesma ausência, achada no E224 e de novo pelo agente do manual da noiva. Conferido em `visao-noiva.ts:188`: existe `retiradaPrevista` e **não existe campo de devolução** |
| ~~**S-C202**~~ | ~~**Das seis cláusulas de dinheiro, só a 9ª desce ao portal.** Avaria, atraso, extravio, rescisão e peça exclusiva não têm seção nenhuma lá — o manual da noiva passou a explicá-las, e a tela dela segue muda~~ | 🔵 | S-C96 | ✅ E230 `f88802d9` — a seção "O que o seu contrato prevê": as seis cláusulas de dinheiro, cada número saindo da CONSTANTE que a conta usa (`clausulas-do-portal.ts`, onze do `financeiro-core`). O teste prega a DERIVAÇÃO, não o valor (a lição da S-C212 pelo lado da tela), e uma peneira recusa qualquer dinheiro que não saia de `brl()` de constante conhecida |
| ~~**S-C210**~~ | ~~**Perdoar a multa da 9ª não tem tela** (irmã da S-C191, achada pelo outro lado). Rotas em `contratos.ts:2352` e `:2429`, hook gerado com 20 ocorrências, **0 usos no frontend** — e o **selo** do perdão já é desenhado (`noiva-portal.tsx:691`). A tela sabe mostrar o resultado de um gesto que ninguém pode fazer~~ | 🟡 | S-C96 | ✅ E226 `6d1cf08a` — "Perdoar multa" na linha da parcela do carnê, sob a régua do P6 (`podePerdoarMora`: o botão só existe onde há o que perdoar, em vez de levar ao 422 `SEM_MORA`); o motivo é obrigatório porque é dele que o portal e o carnê desenham o selo, e perdoada o gesto vira "Restabelecer cobrança" |
| ~~**S-C211**~~ | ~~**A antecedência da 18ª não tem campo, e por isso a cláusula NUNCA dispara.** `prazo_devolucao_reserva_dias`: **0 em `pages/`** (79 arquivos), 0 em `e2e/`, 3 em `routes/`. O E217 fez a conta e declarou que sem prazo preenchido a cláusula não dispara — medido agora, ela não dispara para ninguém, **por falta de campo, não por decisão**. É o E222 pela terceira vez nesta trilha~~ | 🟡 | S-C96 | ✅ E227 `19296ca1` — o campo entra no diálogo "Retirada e devolução" do E224 (o prazo conta DA RETIRADA para trás) e a ficha o lê fora do diálogo ("· 18ª: N dias"). Vermelho literal: `element(s) not found`. As duas portas já estavam prontas — o épico inteiro coube num `<input>`, e é isso que a sobra denunciava |
| ~~**S-C212**~~ | ~~**`MULTA_DA_PECA_EXCLUSIVA_PERCENTUAL` é constante que ninguém lê.**~~ `rescisao.ts` não a importava: a 12ª retinha a fração exclusiva inteira, escrita à mão. Os dois leitores eram o teste que afirma `toBe(100)` — constante confirmando a si mesma — e a régua dos manuais. **A sobra ficou mais cara por causa do commit que a achou**: os manuais deste lote citam a constante numa célula pregada, e a célula PARECIA pregada sem estar | 🟡 | S-C96 | ✅ `99c8064b` — a 12ª passou a derivar dela, e com 100 o resultado é idêntico, o que torna a troca segura de medir. Vermelho encenado porque conserto que não muda resultado precisa provar que a régua passou a morder: constante em 50 e a suíte acusando `AssertionError: expected 428.57 to be 857.14` no caso do E217 |
| ~~**S-C214**~~ | **`CONVITE_TTL_MS` é citado solto em `<p>` ("vale 7 dias") — a régua conhece a constante e só lê `<td>`** | 🔵 | S-C96 | ✅ no commit do lote — **a régua aprendeu a ler prosa anotada** (`<span class="prazo" data-regua>`) em vez de a frase ser empurrada para dentro de uma tabela. Com isso a garantia nova *"toda régua do registro é prometida por algum manual"* passa a valer **sem exceção nomeada** |
| **S-C213** | **Dois recados da tela não são citáveis literalmente** — um é montado com dado interpolado (`reserva.ts:152`), outro está partido em três linhas de JSX (`reservas/[bloqueioId].tsx:945-947`). A `varredura-manuais-textos` exige o literal, e o `data-tela` é a forma certa; a contagem de moldes é `toBe(9)`, igualdade compartilhada, então quatro agentes paralelos não podiam mexer nela | 🔵 | S-C96 | aberta |
| ~~**S-C220**~~ | ~~**A S-C91 nasceu nomeando "quem atende" e a Recepção é a única pessoa que NÃO recebeu o que ela entregou.** As datas de retirada e devolução na ficha derivam de `contratosDaNoiva`, que para a Recepção é `[]` desde o E172. Não se fecha com código: tirar as datas de dentro do módulo `contratos` é decisão da dona, que foi quem o fechou para ela. População: 733 contratos, 1 com `dataRetirada`, 0 com `dataDevolucao` — armado, não disparado~~ | 🟡 | S-C96 | ✅ E229 `329faa98` (decisão 14/08: vê só as datas) — nasceu a LEITURA ESTREITA: `GET /leads/:id/locacao` sob o módulo `leads`, com as duas datas do contrato ATIVO e nada de dinheiro; o teste ENUMERA as chaves do payload (campo novo é decisão, não deriva) e prova no mesmo caso que a porta larga segue 403. A ficha trocou de fonte para TODOS os perfis (uma grafia, E187), e a régua da S-C91 acusou a troca — o pino foi reescrito com a razão |
| **S-C221** | **A Costureira edita o expediente da cláusula 4ª** (`agenda.ts:258`) sem ver contrato nem movimentação de peça. Não medida a intenção da dona — é pergunta, não defeito | 🔵 | S-C96 | aberta |
| **S-C222** | **As três varreduras de manual olham do manual para FORA; nenhuma pega contradição INTERNA.** Foi assim que o contorno do E196 sobreviveu dez épicos no manual da costureira, contradizendo o próprio documento em dois outros pontos, com a suíte verde o tempo todo. O molde da quarta régua já existe em `varredura-manuais-textos` | 🔵 | S-C96 | aberta |
| ~~**S-C233**~~ | ~~**A peça devolvida de um contrato CANCELADO não ganha janela de LAVAGEM.** O braço "na rua" do E225 solta no instante da devolução; no bloqueio vivo, a lavagem ocupa `lavagemDiasDepois` depois dela. Se toda peça devolvida passa pela lavanderia, a do contrato morto pode ser prometida para o dia seguinte. É pergunta de operação antes de ser predicado~~ | 🔵 | E225 | ✅ E228 `3e2ecd8d` (decisão 14/08: passa pela lavanderia) — o braço "na rua" aceita devolvida dentro do teto (`TETO_DA_CAUDA_DE_LAVAGEM_DIAS = 30`, pré-peneira SQL) e as janelas decidem com a regra da loja. Vermelho: 201 para dentro da lavagem; verde: 409 `LAVAGEM` |
| **S-C234** | **A janela de PROVA de uma CANDIDATA varre o passado.** `janelasDoBloqueio` não corta em `hojeDia`: reservar para daqui a 3 dias conflita com USO/LAVAGEM de peça que já voltou, sobre dias em que nenhuma prova vai acontecer. Custa um 409 conservador; cortar `[inicio, hoje−1]` da PROVA da candidata mexe no motor de conflito e merece medição própria. Medido no E228 | 🔵 | E228 | aberta |
| **S-C232** | **A nota do E224 promete o que o código não faz**: *"o campo esvaziado manda `null` de propósito"* — e o código manda `?? undefined` (`[id].tsx`, `onSalvarLocacao`), então **apagar uma data gravada não é possível** pelo diálogo, e o `ContratoUpdate` do spec nem aceita `null`. O prazo da 18ª (E227) herda o limite, declarado desta vez. Classe do E186: nota descrevendo capacidade que o código não tem. Fechar pede spec + codegen (lição da S-C150) | 🔵 | E227 | aberta |
| **S-C231** | **Os cartões-resumo da carteira somam SEM a mora que as linhas abaixo deles mostram.** `receber.tsx:179-181`: `aReceber` soma `saldoAberto` e `emAtraso` vem de `vencidas()` — depois do E226 a linha da parcela vencida diz R$ 515,00 e o cartão em cima soma R$ 500,00, o formato exato da S-C200 uma tela adiante. **Não fechou de passagem porque não é só tela**: os cartões compartilham convenção com a projeção do dashboard e o sino (`projecao.emAtraso`), e mudar um sem os outros trocaria uma divergência dentro da tela por uma divergência entre telas. Decidir se a projeção de caixa da loja conta a mora é pergunta que merece a própria medição | 🟡 | E226 | aberta |
## Pendências que não são software

Nenhuma delas se fecha com código, e as três são da dona:

| # | O que | Por quê |
|---|---|---|
| **P1** | **Corrigir a página 6 do molde** — o CNPJ da assinatura (31.897.111/0001-76) sai; entra o 37.771.644/0001-93 | Decidido em D1. E não é erro de digitação: os **dois números passam na validação de CNPJ**, então a página 6 traz a inscrição de outra empresa |
| **P2** | **Olhar os contratos JÁ ASSINADOS** com a página 6 | Eles carregam o CNPJ errado. Quantos são, e o que fazer, é decisão jurídica |
| **P3** | **Preencher os dados reais da loja** em *Configurações → Dados da loja* | O banco ainda tem o exemplo (`12.345.678/0001-99`, "Rua das Noivas, 123"), e o real está no contrato: Rua Luis Jacinto 297, Centro, São José dos Campos, CEP 12243-260. **O lugar é a tela, não o código** — o seed é parametrizado por env de propósito |

| **P4** | **Escolher o índice da correção monetária** da cláusula 9ª | A cláusula manda corrigir e **não nomeia índice**, e IPCA, IGP-M e INPC dão três números para a mesma dívida — escolher um por conta própria seria inventar cláusula. O E213 cobra a multa e os juros e **declara** que não corrige, na tela e na trilha: *"Sem correção monetária — o contrato não nomeia índice."* Enquanto a decisão não vier, a ausência fica dita em vez de calada |

Uma quinta, menor: a frase de fecho do molde está truncada (*"em duas vias de
igual"*, faltando "teor e forma").

## Decisões ainda abertas

| # | Pergunta | Trava |
|---|---|---|
| **D4** | O PDF do sistema deve virar o instrumento com as 21 cláusulas? | **E220** |
| **D7** | Representante legal (Renato) e chave PIX (CPF de Karina) entram no cadastro da loja? | **E220** |

As duas são da Onda D, e travam **só o E220**. **Podem ser respondidas depois** —
as ondas A, B e C não esperam por elas, **e o E221 também não esperava**: a
ordem sugerida ("D4 e D7 respondidas → E220 e E221") agrupava o recibo com o
instrumento por os dois serem papel, e papel não é uma dependência. O recibo
imprime nome, CNPJ, endereço e telefone, que já estão no cadastro há muito.

---

## O que este plano custa, medido e não estimado

A sessão de hoje deu duas referências reais: o **E198**, o épico mais barato do
plano das sobras, levou **~50 minutos**; o **E199**, ~40. Os dois eram de régua,
sem migração e sem tela.

**Os 12 daqui são de outra natureza.** Nove pedem **migração**, quase todos
tocam **tela**, e sete mexem em **dinheiro** — que é a área onde este repositório
exige exemplo numérico em todo achado e E2E antes do commit (regra 11).

Não vou fingir uma estimativa em horas. O que dá para afirmar com o que foi
medido: **é um plano de dias, não de uma sessão**, e a Onda C sozinha (as três
com migração grande) pesa mais que as ondas A e B juntas.

## O outro plano continua aberto, e isso é uma escolha a fazer

Há **duas frentes abertas ao mesmo tempo**:

| frente | o que resta |
|---|---|
| **Zerar as sobras** (`2026-08-13-zerar-as-sobras-plano.md`) | **25 sobras**, épicos E200–E209 |
| **O contrato vira regra** (esta) | **12 épicos**, E211–E222 |

Elas não competem por código — quase não se cruzam —, mas competem por sessão.
**A recomendação é o contrato primeiro**, e a razão é que as duas frentes têm
naturezas diferentes: as 25 sobras são **dívida de medição** (25 das 26 são 🔵,
e a maioria é régua que mede menos do que anuncia); o contrato é **regra de
negócio que o ateliê já assinou e o sistema não cumpre** — inclusive dinheiro
que ele deixa de cobrar.

Dívida de medição não muda nada para quem usa o sistema hoje. O reajuste da 17ª
muda.

## A ordem sugerida

**Os itens 1 e 2 estão FEITOS, e com eles a Onda A inteira.** O que vem primeiro
agora é o **E222**, pela razão do item 3 — e o mecanismo que os três da Onda A
estabeleceram (*"uma cobrança nasce de um fato do contrato"*, com a conta
DERIVADA e o fato datado no banco) é o que a Onda B reusa. Ele já foi escrito
três vezes: reajuste (E211), atraso (E212) e mora (E213).

1. ~~**E211** — a data que muda tem preço.~~ ✅ `0c8874a`
2. ~~**E212** e **E213** — mesma natureza, mesma fonte de dado.~~ ✅ `a88d7ead`
   e `fa7d838`
3. ~~**E222** — a única cláusula em que o sistema **deixa acontecer** o que o
   contrato proíbe.~~ ✅ `31422db`
4. ~~O resto da Onda B: **E218**.~~ ✅ `f8ab561` — com ele **as Ondas A e B
   estão fechadas**, menos o **E219**, que segue **bloqueado**: a porta que ele
   guardaria não existe, e a linha dele na fila traz a enumeração que prova.

   **Três épicos seguidos ensinaram a mesma coisa, e ela vale para os que
   faltam: o plano deste contrato supõe portas que o sistema não tem.** No E213
   a régua faltava na porta AO LADO (o `POST /receber` recusava o dinheiro que
   as outras três leituras mostravam); no E222 o campo existia e **nenhuma tela
   o oferecia**; no E219 a porta **não existe**. A primeira pergunta de cada um
   dos que restam passa a ser *quantos passam por aqui hoje* — e ela se responde
   com `git ls-files` e um `SELECT`, antes de qualquer linha.
5. ~~**E215** → **E216** → **E217**, nesta ordem (o E217 depende do E216).~~ ✅ `c3adcf93`,
   `eaa4e90`, `a2ada8aa` — **a Onda C está executada**, menos o E219 (bloqueado).
6. **D4** e **D7** respondidas → **E220** e **E221**. O E221 já está ✅ `fcc24e9`
   (não esperava D4/D7 — o plano errou a dependência, ver a nota dele). Resta o
   **E220**, o único item vivo desta ordem.
