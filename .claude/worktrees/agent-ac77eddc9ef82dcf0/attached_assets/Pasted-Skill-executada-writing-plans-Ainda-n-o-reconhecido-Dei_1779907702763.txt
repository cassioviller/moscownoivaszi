Skill executada
/writing-plans
Ainda não reconhecido. Deixa eu checar o nome real da skill no arquivo — pode ser que o nome registrado seja diferente da pasta.

Ler
SKILL.md
O nome está certo (writing-plans), então não é erro de digitação — é mesmo a questão do registro: o Skill tool só enxerga skills carregadas no início da sessão, e o Claude Code não foi reiniciado ainda.

Mas tem um caminho melhor que ficar esperando: a skill é só um arquivo de instruções. Eu posso ler o SKILL.md completo e executar o processo do writing-plans diretamente — seguindo a metodologia dele à risca, sem depender do carregador. Assim não pulamos o passo nem precisamos parar pra reiniciar.

Ler
SKILL.md
I'm using the writing-plans skill to create the implementation plan.

Scope Check (passo obrigatório do writing-plans)
A skill manda verificar: se o spec cobre múltiplos subsistemas independentes, quebrar em planos separados — um por subsistema, cada um entregando software funcional e testável por si só.

A Base, mesmo sendo um sub-projeto, tem subsistemas independentes o bastante para justificar planos separados. Tentar colocar tudo (setup + banco + auth + 5 módulos de config + motor + 3 telas de operação) num único plano com código TDD completo em cada passo geraria um documento gigante e difícil de executar.

Proponho 5 planos sequenciais para a Base:

#	Plano	Entrega isolada e testável
A	Fundação	Next.js + TS + Prisma + Postgres rodando; schema do banco; migrations; seed inicial
B	Motor de Disponibilidade	funções puras + suíte de testes (sem depender de banco/tela) — o núcleo crítico
C	Auth + Multi-loja + Perfis	login, sessão, escopo por loja, permissões por módulo
D	Configuração (CRUDs)	Lojas, Usuários, Catálogo de Atributos, Regras
E	Operação	Lead, Interesses, Vestidos + integração do motor nas telas
Cada plano vira seu próprio arquivo em docs/superpowers/plans/ e é executado antes do próximo.

Qual escrevo primeiro?

Perguntado
1º plano
Por qual plano de implementação começamos?

A — Fundação (recomendado)


Executado 2 comandos, criado um arquivo, usado 3 ferramentas
Apliquei a metodologia do writing-plans diretamente (lendo o SKILL.md), então não pulamos o passo mesmo sem o restart.

Plano A — Fundação: pronto e commitado
docs/superpowers/plans/2026-05-27-base-plano-a-fundacao.md

São 8 tarefas em passos pequenos (TDD, caminho de arquivo exato, código completo, commits frequentes):

#	Tarefa
1	Scaffold Next.js + TS + Tailwind
2	PostgreSQL local (docker-compose) + env
3	Instalar e inicializar Prisma + client singleton
4	Schema completo da Base (todas as 13 entidades + enums)
5	Primeira migration
6	Configurar Vitest
7	Seed inicial (loja, perfis, admin, catálogo, regras)
8	Smoke test do seed
Fiz a auto-revisão exigida pela skill: cobertura do spec ✓, sem placeholders ✓, consistência de tipos entre schema/seed/teste ✓.

Execução — como você quer rodar este plano?
Perguntado
Execução
Como executar o Plano A (Fundação)?